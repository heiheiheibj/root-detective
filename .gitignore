# 依赖与构建产物
node_modules/
dist/
.vite/

# 数据管线的中间产物（可重新下载/重新生成，见 docs/扩词库计划.md）
# raw/ 是原始下载，derived/ 是中间件，quarantine/ 是被判不合格的词条。
# 全部可复现，不进仓库；只有 scripts/gates/ 下的白名单和 overrides/ 要进。
scripts/.work/

# 本机备份归档，18MB 二进制，进仓库只会拖慢每次 clone
背单词.7z

# 编辑器与系统
.DS_Store
Thumbs.db
*.log

# 一次性探针脚本与临时产物（历史遗留已解除跟踪，本地保留）
# 名字以 _ 开头的是「跑一次就扔」的调试脚本，不进仓库；需要长期复用的请放进 scripts/tools/
_*.mjs
_*.tsv
_*.txt
_*.ps1

# 浏览器自动化产物（playwright-cli / chrome-devtools 的页面快照与无障碍树）
.playwright-cli/

# 根目录的临时截图：验证留档统一放 docs/shots/，不要散在根目录
/*.png
# 误提交的历史杂物（坏文件名、编辑器另存的副本）
console.log('ERR'
* - 副本.json
/*.yaml
