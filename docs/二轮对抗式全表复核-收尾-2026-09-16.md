# 二轮对抗式全表复核 · 修复收尾（2026-09-16）

> 承接 `复核修复进度-2026-09-15.md`（一轮修了 408 条可见错注）。
> 本轮对 **全表 2698 词逐词逐字面对抗式复核**（13 批 × 200 词，复核记录落盘
> `scripts/.work/review/problems-01..13.json` + `words-review.json`），核出 **319 个 major**
> —— 全部是「字面义当时按正确词源写、词素表却注错」的对不上，即在源头改义项表/切分表，
> 重建后机械自然转绿。**最终：2706 词、0 错误、144 警告、`npm test` 54/54、漂移 0.0%。**

## 一、复核发现（major 319 / minor 137，全部记录在案）

典型类别（完整清单见 problems-01..13.json 的 issues 字段）：

| 类别 | 例子 |
|---|---|
| 词典把「现代词义」当词素义 | mortal=生物（该是 必死的、凡人）、second=秒（该是 第二）、pay=薪资（该是 支付）、suit=套装（该是 跟随/起诉）、dorm=宿舍（该是 睡） |
| 专名/药名/乐器/朝代泄漏 | eros=爱神、vicin=巢菜碱、dox=强力霉素、violat=中提琴、ming=明朝、mars=火星、orc=虎鲸 |
| 残句泄漏（混进干扰项选项文案） | natural=白痴自然的、marx=马克思无产阶级的、rough=粗糙的东西、scramb=英方用指甲或爪子、epi=直沙嘴、inverse=以诗体 |
| 双支词源只挂了一支 | hat=憎恨（帽子）、leg=腿（读）、court=法院（宫廷）、ship=身份（船）、car=关心（车）、plain=平原（捶胸） |
| 切分指错词素 | translate=tran+slate、transport=tran+**sport**(运动!)、company=comp+any、healthy=heal+**thy**(你的)、command=comm+and、decline=dec(十)+line、nowhere=now+**here**(她!)、farther=**fart**+her、reproach=**rep**(棱纹平布)、denounce=**den**(兽穴)+**ounce**(盎司)、descent=des+**cent**(百)、emigrate=emi+**grate**(高兴、感激!)、refrain=ref+**rain**(雨!)、terrain=ter(三次)+**rain** |

## 二、源头修复（全部落盘）

### 1. `scripts/lib/morpheme-fallback.mjs`
- **OVERRIDE_MEANINGS 追加 ~310 条**（总 722 条）：junk 义项直接删，真双支以「；」并立。
- **INJECT_MORPHEMES 新增 10 个**（总 17 个）：cline(倾斜)、liber(称量/天平)、electr(电)、
  ther(比较级后缀)、far(远)、frain(勒住)、scent(气味；爬)、terr(地)、ain(法语词尾)、
  ade(饮料)、chill(寒冷整词)、sole(单独；脚掌整词)。
- key 口径修正：部分 key 按「表面」写会静默失效（自检报「上游不存在」），
  `resolve-ids.mjs` 把它们重写成真实 morphemeId。

### 2. `scripts/tools/build-stage3-config.mjs`
- **SPLIT_REPLACE 新增 27 条**（总 32 处）：上表切分错误全部重挂正身。
- `touched` 集合纳入 SPLIT_REPLACE 引用的既有词素 id —— 它们的新表面
  （ad[an]、ne[no]、roach[proach]、wool[wooll]…）必须收进变体表，否则 21 号 A6 拒收。
- **TYPE_FIX**：pan/wide/where 改判 root（cigen 误标 prefix/suffix），company/wide/nowhere
  由此过 A18；root 色改 orange（A20），并按词义挂进主题世界
  （pan→炉火坊、wide→度量台、where→四方塔，A24）。

### 3. 受义变波及的内容修复
- **252 个干扰项重生成**（`scripts/.work/review/fix-metaphor-options.mjs`）：旧干扰项是从
  垃圾义项生成的（如「自动手枪的一角」），义项纠正后失去 A12 依据，按现行义项池重算，
  模板沿用语料现有「错的画面」句式，并过滤 A28 元话语。
- **8 个新词入表**：lemonade/somewhere/panic/wherever/immigrate/whereas/widely/whereby
  此前一直被 A6/A18 拦下，词素表修正后首次通过 —— 补 prose（batch-49.json）与
  手写例句（words-examples-stage3c.json，22 号已注册该层）。
- **retard 例句替换**：原 Tatoeba 句用了冒犯义「笨蛋」，换成 "Growth was retarded by the
  long drought."（本词教的是「延缓」）。

## 三、最终状态

- `npm run content:all` 端到端全绿：**内容校验通过 2706 词、2137 词素（含 1840 词根）、42 世界，
  0 错误**；切分通过 2706（丢 0）；60 号复核闸通过（major 0 / minor 137 全部记录在案，漂移 0.0%）。
- `npm test` **54/54 通过**。
- **收尾补刀**：A20b/c 抓到两轮复核都漏掉的 `vis:医力`（ECDICT 缩写词条污染，会污染
  6 个词的拼词盘干扰牌）—— 它本是 vid(看) 的变体卡（advise/supervise 落在它上面），
  MORPHEME_MERGE `vis→vid` 归并，词素 2137→2136、警告 144→142。
- 144 条警告构成：A23 难度梯度（新增 8 词带来个位数增量，选题层面）、A8 例句专名、
  A22 canary 锚点变体 —— 均为已知合理保留。
- 复核工作台（problems/阅读批/finalize 脚本）在 `scripts/.work/review/`，一次性工具不进产物。
