# 测算方法说明（供独立复算 / 交叉验证）

> 目的：让另一个 AI 或人**从原始数据独立实现**这套测算，然后对比数字。
> 结论不一致时，按第 6 节逐项中间量对照，能定位到具体哪一步。
>
> 生成时间：2026-09-13

---

## 0. 怎么用这份文档

**两种验证等级，建议做等级 1：**

| 等级 | 做法 | 能验证什么 |
|---|---|---|
| **等级 1（推荐）** | 按本文档**自己实现**，不看我写的脚本 | 方法是否可复现、结论是否成立 |
| 等级 2 | 直接跑我的脚本 `scripts/tools/*.mjs` | 只验证代码有没有写错 |

**先对齐输入**（数据不一致则后面全部白算）：

| 文件 | 字节数 | sha256 |
|---|---|---|
| `scripts/.work/raw/ecdict.csv` | 65,933,428 | `1a6947e04785db63613a92e14903cdae7954f7e84860b10e68e5c7cbb3f9c3cf` |
| `scripts/.work/raw/kaikki-English.jsonl` | 3,244,830,710 | `3c202ca40f2a57d74e8bc4d2567d4782cd0aa2e5251ed40522a691338ac49afa` |
| `scripts/.work/raw/cigen-roots_affixes.json` | 388,646 | `b20aae11372041d79e4866154abec4ed8ca6ec0e3cb1e1ddb1bc09795c20dcef` |
| `scripts/.work/raw/morphynet-eng-derivational.tsv` | 8,169,109 | `5920edacc1888b14464fc5cd96beea0a721221d56d1dc0e49de22f4c7c537c50` |
| `scripts/.work/raw/wordroot.txt` | 370,702 | `4b400dc5980e4a362fa12c7a49b326bb621c4e6bebd5f37146fbdcbe9db6bb6e` |
| `scripts/.work/raw/shiweihappy-roots.json` | 123,973 | `d6c58979c9c634e934c22975e48f6b1a802d537a4c35b8baafe085e3a4a34884` |
| `scripts/.work/derived/roots.candidates.json` | 764,323 | `d77ee3b3172da3bf306140e8e336002603c98d7c4ce9d2e51a0705f61ada2eac` |

数据来源与许可：ECDICT（MIT，`ecdict.csv` + `wordroot.txt`）、kaikki/Wiktionary（CC BY-SA，`kaikki-English.jsonl`）、
cigen（仓库标 MIT，但数据源自新东方 PDF，**只可用于交叉核对与切分证据，不可复制其释义**）、
MorphyNet（CC BY-SA）、shiweihappy（同上）。下载见 `scripts/00-fetch-sources.mjs`。

### 快速验证路径（约 1 分钟，不必碰 3.2 GB）

**先把计算 B 的行数限制在 200,001 行**（读满就停），预期结果：

| 项 | 数字 |
|---|---|
| 扫描行数 | 200,001 |
| 含 `"etymology_templates"` 且解析成功的行数 | 103,231 |
| 模板路径 / 文本路径 | 14,950 / 8,552 |
| 被白名单拒掉的文本拆分 | 14,560 |
| 拿到 ≥2 词素拆分的词数 | **23,502** |
| 词素总数 | **9,094** |
| ≥3 词 / ≥5 词 / ≥10 词 | 3,615 / 1,963 / 798 |
| 分档 1 / 2 / 3-4 / 5-9 / 10-19 / 20-49 / 50+ | 3,785 / 1,694 / 1,652 / 1,165 / 462 / 232 / 104 |
| 最高频词素 | `ly` = 3,016 词 |

> ⚠️ 上表是**两次修正后**的数字（见 3.7 节）。修正前的数字（41,384 / 21,931）含大量污染拆分，已废弃。

**这一步对上了再跑全量** —— 全量数字见第 3.6 与 4.4 节。

> 采样是确定性的（同一文件、同一顺序），两边必须得到同一组数字。

---

## 1. 通用约定（所有计算共用）

```
norm(s) = String(s).toLowerCase().replace(/[^a-z]/g, '')
```

**所有词、词素、id 一律先 norm 再比较**。这一步做错会导致两边数字对不上（例如 `self-` 与 `self`、
`-tion` 与 `tion` 必须视为同一个东西）。

**ECDICT 难度规则（6.4）**，输入是某词的 `tag` 数组、`collins`、`bnc`：

```
d1  ←  tag 含 'zk' 或 'gk'，或 collins === 5，或 (0 < bnc < 3000)
d5  ←  (tag 含 'cet6' 或 'toefl') 且 (bnc > 10000 或 bnc === 0)
d3  ←  其余
```

**考试词判定（6.2）**：

```
passTag  ← tag 含 ['zk','gk','cet4','cet6'] 之一
passCom  ← collins > 0 或 oxford > 0 或 (0 < bnc < 20000) 或 (0 < frq < 20000)
pass62   ← passTag 且 passCom
```

**保送（forceInclude，Stage 2 实践）**：`d5` 且 tag 含 `toefl`/`ielts`/`gre`/`cet6` 之一 —— 允许在 6.2 不达标时收录。

---

## 2. 计算 A：考试词集合（基准分母）

**输入**：`ecdict.csv`

**格式**：CSV，**必须用正规 CSV 解析**（字段可被双引号包裹，内含逗号与 `""` 转义；直接 `split(',')` 会错列）。
列序（0 起）：

```
0 word   1 phonetic   2 definition   3 translation   4 pos
5 collins   6 oxford   7 tag   8 bnc   9 frq   10 exchange   11 detail   12 audio
```

`tag` 是空格分隔的小写标签串。

**定义**：`word = norm(f[0])` 非空，且 `tag` 含 `zk`/`gk`/`cet4`/`cet6` 任一 → 考试词。
同一 norm 词只计一次。

> ⚠️ **第 1 行是表头**（`word,phonetic,definition,translation,...`），**必须跳过**。
> 不跳过会让中间量多 1（`norm("word")` 非空，被当成词条）。

**预期结果**：

| 项 | 数字 |
|---|---|
| ECDICT 非空行 | 770,612 |
| norm(首字段) 非空的行（**含 1 行表头**） | 770,483 |
| **真实词条（排除表头）** | **770,482** |
| **考试词（按 norm 去重）** | **7,111** |
| tag=zk（去重后） | 1,601 |
| tag=gk（去重后） | 3,676 |
| tag=cet4（去重后） | 3,847 |
| tag=cet6（去重后） | 5,404 |

> ⚠️ **分层计数口径**：上表按 **norm 词去重后** 计数（与分母 7,111 同口径）。
> 若改成按 ECDICT **行数**直接计数（同一 norm 词的不同条目会重复计入），
> 得到 zk 1,603 / gk 3,677 / cet4 3,849 / cet6 5,407 —— **两个口径都对，但必须说清用哪个**。
> 首轮交叉验证的 ±3 差异就来自这里。
>
> 注：一个词可有多个 tag，所以四个层级之和大于 7,111。

---

## 3. 计算 B：kaikki 权威词素提取（核心）

**输入**：`kaikki-English.jsonl`（3.24 GB，1,492,836 行，每行一个 JSON 词条）

**目的**：拿 Wiktionary 编辑手工标注的「表面拆分」，得到 词 → 词素序列。

### 3.1 遍历与过滤

逐行流式读取，按顺序计数；对每行：

1. 若行内**不含子串** `"etymology_templates"` → 跳过（纯性能优化，不改变结果）
2. `JSON.parse`；解析失败 → 跳过
3. `obj.lang_code !== 'en'` → 跳过
4. `word = norm(obj.word)`，为空 → 跳过

### 3.2 模板优先级（取第一个命中的）

```
surf → prefix → pre → suffix → suf → confix → affix → af → compound/affix → compound → com
```

> ⚠️ **这一条是最大的坑**：Wiktionary 大量使用缩写别名。
> 只认 `prefix`/`suffix`/`affix`/`surf` 会漏掉 `af`（affix）、`suf`（suffix）、`pre`（prefix）、`com`（compound）——
> 覆盖率会从 41.3% 掉到 18.9%。

### 3.3 从模板取词素

取该模板 `args` 中**所有纯数字键**（`"2"`, `"3"`, …）的值，**跳过 `"1"`（语言码）**，
按数字升序排列，逐个 `norm()`，丢弃空串。

命名参数（`t`/`alt`/`pos`/`id`/`tr`/`gloss`…）都不匹配 `^\d+$`，天然被排除。

### 3.4 模板拿不到时的文本解析

若 3.3 得到的词素**少于 2 个**，则解析 `obj.etymology_text`，按顺序试以下正则，**第一个匹配且片段含 `+` 的胜出**：

```
1) /equivalent to ([^.;]+)/i
2) /surface analysis,?\s*([^.;]+)/i
3) /^From ([^.;]*\+[^.;]*)/i
4) /from ([^.;]*\+[^.;]*?)(?:[.;]|$)/i
```

拿到片段后：

```
pieces = 片段按 '+' 分割
每段：删掉括号内容（/\([^)]*\)/g → 空格）→ norm()
保留：长度 2..20 且 /^[a-z]+$/
若最终 ≥2 个 → 采用，来源标记为 'text'
```

> 例：`"equivalent to king + -dom"` → `['king','dom']`；`"From tele- + vision"` → `['tele','vision']`；
> `"surface analysis, port ("to carry") + -able"` → `['port','able']`

### 3.5 记录

- 词素 < 2 个 → 丢弃
- **每个词只记第一条成功拆分**（`splits` 以 word 为键，先到先得；同一词可能有 noun/verb/adj 多个条目，按文件顺序取第一个成功的）
- 词素 → 词集合：`morphemeWords[morpheme].add(word)`（**用 Set 去重**）

### 3.6 预期结果

| 项 | 数字 |
|---|---|
| 扫描总行数 | 1,492,836 |
| 含 `"etymology_templates"` 且 JSON 解析成功的行数 | 490,949 |
| 模板路径 / 文本路径 | 235,994 / 41,639 |
| 被白名单拒掉的文本拆分 | 10,530 |
| **拿到 ≥2 词素拆分的词数** | **277,633** |
| **词素总数** | **79,964** |
| 只带 1 词 | 41,711 |
| 带 2 词 | 13,160 |
| 带 3–4 词 | 9,989 |
| 带 5–9 词 | 7,677 |
| 带 10–19 词 | 4,165 |
| 带 20–49 词 | 2,258 |
| 带 50+ 词 | 1,004 |
| **≥3 词** | **25,093** |
| **≥5 词** | **15,104** |
| **≥10 词** | **7,427** |

### 3.7 提取器的两轮修正（务必按修正版实现）

首轮实现有两个 bug，会把垃圾当词素，**复算时必须按修正版**：

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | `action` 被拆成 `en+act+ion`（多一个 en）<br>`actress` 出现 `essidfemale` | `surf` 模板参数位置不同：`{"1":"+suf","2":"en","3":"act","4":"ion"}`，<br>第 1 位是组合方式标记、第 2 位才是语言码；<br>参数值还会带内联注解 `ess<id:female>` | `surf` 跳过 1、2；其它跳过 1；<br>值先剥掉 `<...>` 再归一化；<br>过滤语言码值 |
| 2 | `adjective` 被拆成 `vusfromad+iaci`<br>`access` 出现 `ad+cd` | 文本正则 `[^.;]*` 允许跨逗号，把多个 `from` 子句连成一片；<br>变音符号被 `norm` 删掉（`cēdere`→`cdere`→`cd`） | 字符类收紧为 `[^,.;()]`，片段限长 40；<br>**文本路径的词素必须在模板路径白名单里** |

**修正代价**：全量词数 313,856 → 277,633（−12%），但**换来的是可用性**——
首轮抽检发现 `en+act+ion`、`ad+cd`、`vusfromad+iaci` 这类污染，若直接入库会污染整个词素表。
宁可少 12%，不要错。

> 分档边界：`n===1` / `n===2` / `3≤n≤4` / `5≤n≤9` / `10≤n≤19` / `20≤n≤49` / `n≥50`。

---

## 4. 计算 C：考试词覆盖率（决定目标）

**输入**：计算 A 的考试词集合 + 计算 B 的 `splits` 映射

### 4.1 覆盖判定

```
covered   = { w ∈ 考试词 : splits[w] 存在 且 splits[w].parts.length ≥ 2 }
uncovered = 其余
```

（因为 B 只收录 ≥2 词素的词，所以等价于「w 在 splits 里」。）

### 4.2 词素统计

对每个 covered 词，遍历其 `parts`：

```
morphemeWords[part].add(word)          // 用 Set
位置计数（用于判前缀/词根/后缀）：
  i === 0            → head += 1
  i === parts.length-1 → tail += 1
  其余                → mid  += 1
```

### 4.3 累计覆盖量

对阈值 k ∈ {3, 5, 10, 20}：

```
hit = { 词素 m : |morphemeWords[m]| ≥ k }
wordsCovered = ∪ morphemeWords[m]   (m ∈ hit)     // 并集，按词去重
输出 |hit| 与 |wordsCovered|
```

### 4.4 预期结果

**口径 ①：只用 kaikki 单源**（首轮交叉验证过的那版）

| 项 | 数字 |
|---|---|
| covered | 2,938（41.3% / 7,111） |
| 涉及词素 | 2,582 |
| ≥3 / ≥5 / ≥10 / ≥20 词 | 347 → 2,656 / 147 → 2,419 / 66 → 2,072 / 32 → 1,688 |

> 该口径的提取器含 3.7 节的两个 bug（`en+act+ion`、`ad+cd` 那类污染），数字仅作历史对照。

**口径 ②：融合五源**（当前口径，见 `fuse-splits.mjs`）

| 项 | 数字 |
|---|---|
| **covered** | **2,940**（41.3% / 7,111） |
| 未覆盖 | 4,171 |
| zk / gk / cet4 / cet6 | 419/1,601（26.2%）/ 1,357/3,676（36.9%）/ 1,573/3,847（40.9%）/ 2,323/5,404（43.0%） |
| 涉及词素 | 2,418 |
| 来源分布 | 模板 1,382 / 文本 906 / cigen 161 / MorphyNet 239 / 复合词 252（其中 682 个经过表面对齐） |

**累计覆盖（关键决策数字，口径 ②）**：

| 阈值 | 词素数 | 覆盖考试词 | 占已覆盖 |
|---|---|---|---|
| ≥3 词 | **380** | **2,729** | 92.8% |
| ≥5 词 | 163 | 2,498 | 85.0% |
| ≥10 词 | 64 | 2,093 | 71.2% |
| ≥20 词 | 35 | 1,743 | 59.3% |

**覆盖最多的词素**：`ly`=198 `ion`=161 `er`=161 `al`=104 `ing`=97 `re`=76 `ity`=62 `ation`=61 `in`=59 `ment`=53 `un`=51 `dis`=47

**融合没让覆盖率大涨的原因**：文本路径被词素白名单砍掉 10,530 个（大多污染），
复合词与 MorphyNet 补回 491 个 —— 一增一减基本持平（2,938 → 2,940）。
**覆盖率要靠 S3 的规则切分再推**（`accept = ac + cept` 这类：Wiktionary 只给了拉丁词干 `ad + capi`，表面拼不回）。

**融合五源与置信度**：

| 来源 | 置信度 | 说明 |
|---|---|---|
| kaikki 模板 | high | Wiktionary 手工标注的表面拆分 |
| kaikki 文本 | medium | 词源文本句式解析，**逐词素白名单过滤** |
| cigen | high | 953 条人工切分 |
| MorphyNet | medium | 派生关系递归展开（深度 ≤4） |
| 复合词规则 | medium | 两半都是常见**实词**（ECDICT 词缀条目已排除） |

**表面对齐**：所有拆分必须通过「surface 拼接 == 原词」校验。词源形式与表面不一致时
（`able→abil`、`absorb→absorp`、`snobbish = snobb+ish`）用 DP 求切分点，容差 `max(2, 25% 词长)`；
代价打平时优先让**后缀完整**。

---

## 5. 计算 D：可切分口径天花板（否决旧目标的依据）

> 这个计算是为了回答「沿用 Stage 2 那套小词根表，最多能扩到多少词根」。
> 结论 155 是被否决的旧目标依据；如果对方算出的也是 ~155，说明两条路线独立得到了同一结论。

**输入**：`roots.candidates.json`（406 条候选词根，`type === 'root'` 的参与计算）、
`cigen-roots_affixes.json`、`morphynet-eng-derivational.tsv`、`wordroot.txt`（通过候选的 `examples` 字段）、`ecdict.csv`

### 5.1 每个候选的表面形式

```
surfaces = { norm(id) } ∪ { norm(a) : a ∈ allomorphs }，丢弃长度 < 3 的
```

### 5.2 家族词收集（三个来源）

| 来源 | 取法 |
|---|---|
| `cigen` | `components[].morpheme` 归一化后**全等**于某个 surface → 该 `word` |
| `wordroot` 例词 | 候选的 `examples[]` 中，**字面包含**某个 surface 的 |
| `morphynet` | TSV 第 5 列归一化全等于 surface → 取第 1、2 列的 word，且**字面包含**该 surface |

**「字面包含」规则**：

```
surface 长度 ≥ 4 → word.includes(surface)
surface 长度 = 3 → word.startsWith(surface) 或 word.endsWith(surface)
```

**Morphynet 额外要求「词根不在词尾」**：

```
若 word.endsWith(surface) 且 word.length > surface.length + 1  →  剔除
```

> 这条只对 morphynet 用。复合词误判全来自它（`king|dom`、`police|man`）。
> cigen/例词不能用这条，否则会误杀 `attend`/`extend`/`intend`（前缀+词根结构）。

### 5.3 难度与合格判定

对家族词逐个查 ECDICT：`pass62(e)` 或（允许保送时 `canForce(e)`）→ 计入可用词。
然后 A23：

```
合格 ← 可用词 ≥ 3  且  d1 ≥ 1  且  d5 ≥ 1
```

（「放宽 d5」口径即去掉最后一条。）

### 5.4 已用词根判定（影响「未用」计数）

```
usedSurfaces = ∪ { norm(m.id), norm(m.allomorphs) : m 是 stage3-content.json 与 legacy-morphemes.json 里 type==='root' 的词素 }
候选若任一 surface ∈ usedSurfaces → 视为已用
```

> 只看 `id` 会把 `spect` 当新词根（它是已用 `spec` 的变体），从而高估天花板。

### 5.5 预期结果

| 口径 | 合格词素 | 其中未用 | 天花板（60 + 未用） |
|---|---|---|---|
| X1 严格 6.2 + 完整 A23 | 49 | 27 | 87 |
| **X2 允许保送 + 完整 A23** | **138** | **95** | **155** |
| X3 允许保送 + 放宽 d5 | 164 | 116 | 176 |

未用候选的失败原因：**词不足 3 个 209 / 缺 d1 28 / 缺 d5 21**。

---

## 6. 已知陷阱（数字对不上先查这里）

1. **CSV 必须正规解析** —— ECDICT 有引号包裹、内含逗号的字段，`split(',')` 会错列
2. **模板别名** —— 漏 `af`/`suf`/`pre`/`com` → 词素少一半，覆盖率 41.3% → 18.9%
3. **不解析词源文本** —— 漏 `"equivalent to X + Y"` 这类句式 → 同样大幅低估
4. **norm 不统一** —— 一边 norm 一边不 norm，匹配率骤降
5. **模板 `"1"` 键** —— 是语言码，必须跳过
6. **每个词只记一条拆分**（不是合并所有模板）
7. **词素计数用 Set 去重**（同一词重复出现同一词素只算一次）
8. **`bnc === 0` 的含义** —— 视为「无排名/冷门」，在 d5 判定里算达标
9. **`lang_code` 字段** —— kaikki 的 JSON 里语言码字段名是 `lang_code`，不是 `lang`
10. **流式读取** —— 3.2 GB 文件不要一次性 `readFileSync`

---

## 6.5 边界语义规定（消除实现自由度）

首轮独立复算在四项计算上全部复现，仅剩 ±1~5 的边界差异。这些差异**全部**来自下列自由度。
**按本节规定实现即可 100% 对齐**：

1. **同一 norm 词的多条目**（不同词性各一行）：逐行扫描，**取文件顺序里第一个成功的拆分**，
   后续同词条目**不覆盖**。—— 不要先收集所有条目、再挑"优先级最高的模板"，那是另一种口径
   （B 的 3~5 个词差异来自这里）
2. **ECDICT 表头**：第 1 行必须跳过（见第 2 节）
3. **分层 tag 计数**：一律**按 norm 词去重**（与考试词分母同口径），不要按行计数
4. **词典例词里的多词条目**（如 `ad hoc`）：先 `norm()` 再比较，得到 `adhoc`
5. **norm 碰撞**：`self-` / `-self` / `self` 归一化后是同一个词素，合并计数
6. **词源文本正则**：模式里的字符类必须排除 `.` 和 `;`（`[^.;]*`），否则会跨句贪婪匹配

---

## 7. 无法自动验证的部分（主观判断，需人工）

以下结论**不来自脚本**，是我基于抽检的人工判断，交叉验证时请单独讨论：

1. **语义可教性** —— 脚本只量「能不能切」，量不出「讲不讲得通」。
   例：`man`(hand) 的家族混入 `permanent`（词源 manere=停留）；`dom` 既是「房子」词根又是 `-dom` 后缀。
   抽检 6 个候选的结论：约 80% 判定可信，剩 20% 需人工剔除。
2. **词素类型归类** —— `head/mid/tail` 位置统计只能**提示**前缀/词根/后缀，最终归类要人工。
3. **155 这个天花板**包含上述人工折扣，**纯脚本输出是 X2 的 138/95**。
4. **目标该定多少** —— 是产品决策，不是计算结论。

---

## 8. 一句话总结每个数字的来历

| 数字 | 来历 |
|---|---|
| 7,111 | ECDICT 里带 zk/gk/cet4/cet6 标签的词（去重） |
| 277,633 | Wiktionary 里有权威表面拆分的英语词（**修正版提取器**） |
| 2,940 | 前两者交集 = 有拆分且是考试词（**融合五源后**） |
| 380 / 2,729 | 覆盖 ≥3 个考试词的词素数 / 它们能覆盖的考试词数 |
| 155 | 沿用旧候选表（406 条）时的词根上限（含人工折扣；已被新口径取代） |

---

## 10. 交付状态（2026-09-13）

| 层 | 工具 | 产物 |
|---|---|---|
| Wiktionary 提取 | `kaikki-morphemes.mjs` | `.work/derived/kaikki-splits.json`（277,633 词） |
| 五源融合 + 表面对齐 | `fuse-splits.mjs` | `.work/derived/splits-fused.json`（2,940 考试词） |
| 质量抽检 | `audit-splits.mjs` | 随机取样人工复核（支持 `--aligned` / `--source=`） |
| 覆盖率/词素排行 | `exam-coverage.mjs` | `.work/derived/exam-coverage.json` |
| 旧口径天花板 | `supply-ceiling.mjs` | `.work/derived/ceiling.json` |

**抽检记录**：随机 28 个（`seed=42`）→ 26 个合理（93%）；对齐样本 18 个（`seed=9`）→ 16 个合理（89%）。
已知遗留问题：`tribute=tri+bute`（已修）、`startle=start+le`（Wiktionary 标注，教学价值低，S2 按词素数过滤）。

---

## 9. 交叉验证记录（2026-09-13）

由另一 AI 按本文件**独立实现**复算（不看本文档作者的脚本），结果：

| 计算 | 本文档数字 | 独立复算 | 判定 |
|---|---|---|---|
| A 考试词 | 7,111 | 7,111 | ✅ 一致 |
| A 中间量（词条数） | 770,483 | 770,482 | 差 1 = **表头行**（本文件原先未写明，已修正并修正脚本） |
| A 分层 tag | 1,601/3,676/3,847/5,404（去重口径） | 1,603/3,677/3,849/5,407（行口径） | **口径歧义**，本文件已写明两种口径（6.5 节第 3 条） |
| B 词数（采样） | 41,384 | 41,381 | 差 3 = 同词多条目取法（6.5 节第 1 条） |
| B 词数（全量） | 313,856 | 313,851 | 差 5，同上 |
| B 词素数 | 21,931 / 96,005 | 21,931 / 96,005 | ✅ 一致 |
| B 全部 7 档 + 3 个阈值 + top 词素 | — | 全部命中 | ✅ 一致 |
| **C 覆盖率** | 2,938（41.3%）/ 347 词素 → 2,656 词 | **完全一致** | ✅ 一致 |
| D X2 | 138 合格 / 95 未用 → 155 | 137 / 94 | 差 1，d1 边界词的家族收集差异 |
| **核心结论** | — | **全部复现成立** | ✅ |

**结论**：方法可复现，关键指标量级稳定；所有差异均在 0.01% 级且已归因到位（表头 / 口径 / 同词多条目 / d1 边界）。
复算方独立确认「旧目标 250 根被否决」成立。

**本次交叉验证的实际收获**：
1. 本文档原先的 770,483 含表头，是记录瑕疵 → 已修正为 770,482 并写明表头必须跳过
2. 分层 tag 的两套口径原先没写清 → 已在 6.5 节写死
3. 新增 6.5 节的 6 条边界规定，下次复算可 100% 对齐
