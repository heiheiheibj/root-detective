# 词素义项全表复核 · 修复进度（2026-09-15，可中断续接）

> 任务来源：`docs/复核报告-3adf204至8e721f7.md`（另一个 AI 的独立复核）+ 本轮续查发现。
> 一句话现状：**✅ 已完成并验证** —— 管线全绿：`内容校验通过：2698 词、2146 词素（含 1845 词根）、
> 42 世界，138 警告`（0 错误；修复前 1035 警告），`npm test` **54/54 通过**，
> `npm run content:all` 端到端全绿（含 60/70 号）。
> 剩余 138 条警告全部是已知的合理保留：A23 难度梯度 127（选题）、A8 例句夹专名 9（原有）、
> A22 canary 锚点变体 2（为不破坏 16 个回归锚点而保留）。

---

## 一、复核报告的验证结论（已独立复现，不是照抄）

报告的基线全部复现：`npm test` 53/53、A1 811 / A23 128 / A22 87 / A8 9、A20b=0 / A24b=0、
2698 词 / 2144 词素 / 42 世界、parts 恰好 20 词变化、删 11 个词素 id、
distractors 2682 / metaphorOptions 363 / familyWordIds 34 波及面、无多世界词素、
cor=11（含 correct/correlate）/ mit=10（含 missing）/ jud=8（含 adjust）。

**报告「建议修改」2.2~2.6 逐条属实**；「确认无误」9 条全部成立。

报告本身要改的三处（已记入本文档第五节，尚未落盘）：
1. 2.1 的「扫描方法」不实：它给的 `/单位|药|器|率|缩写|组织|接口/` 复现不出它自己那张表
   （statistic/wandn/whet 不含这些标记；真跑出来的是 organize/poison/bin/rate 等真义项）。
   那批是人工通读挑的，不是正则产物。
2. 3.4 的算式不成立：89 条 OVERRIDE − 13 条 id 已不存在 = **76**，推不出 74。
   实际是「76 条命中现存词素、其中 74 条真的改了值」。
3. 3.7 要换个说法：`scripts/.work/derived/report.md` 是 **Stage 1 遗留产物**（写「词条 300 /
   复核 ok 300」，生成于 09-13），`words.reviewed.json` 也只有 300 条 ——
   是 **70 号/60 号没随 Stage 3 重跑**，不是「2400 词缺复核」。

## 二、本轮新发现（复核报告没看到的）

| # | 发现 | 规模 |
|---|---|---|
| 1 | A20b 的 `/^医/` 行首锚点抓不到**夹在中间**的标记：「枪医枪」「视觉的医视觉的」「必然的事情计计算」 | 10 条 |
| 2 | 干扰项池污染：`distractor.text` 是指向词素表的软外键，垃圾义项会画进**别的词**的拼词盘 | 101 条干扰项 / 99 词 / 20 词素 |
| 3 | **ECDICT 领域标记泄漏**：`shortMeaning` 把 `[医] 山` 的标记当字头拼进义项 → 「医山」「计硬件描象层」 | 165 条（严判） |
| 4 | **缩写/专名/包含匹配当义项**：`wf`←Water Filter、`so`←spin-orbit splitting、`wandn`←insidescrewandnonrisingstem | 17+40+34 条 |
| 5 | **词性段拼接**：ECDICT 的 `n. 雇用 vt. 雇用` 无逗号 → 「雇用雇用」「任何的任何」「女儿女儿的」 | 44 条（机器判据） |
| 6 | 义项与家族词对不上（正则永远抓不到）：`car`=汽车 之于 careful、`bull`=公牛 之于 bullet、`fly`=苍蝇 之于 butterfly… | ~200 条人工核对 |
| 7 | 切分本身切错的词：wander/whether/carrot/isolate/delivery 每个都被切成两个毫无意义的碎片 | 5 词 |

**根因**：零件词素（家族 < 3 的碎片）的义项是 `build-morpheme-table.mjs` 的 `deriveMeaning`
按 id 去 ECDICT 查「这个 id 当英文词是什么」兜出来的 —— 撞上缩写/专名/术语词条就完蛋，
而且旧 `shortMeaning` 不剥 `[..]` 标记、不在词性段中间切，把垃圾拼成合法的 1–8 汉字过 A20。

## 三、已完成的修改（全部落盘）

### 1. 根因修复 `scripts/tools/build-morpheme-table.mjs`
- `shortMeaning`：**先剥 `[..]` 领域标记**，再取首义，**并在词性段中间切一刀**
  （`a. 任何的 pron. 任何一个` → 「任何的」，不再拼成「任何的任何」）。
- `ecMeaning` 记录改存 `{ raw, translation, junk }`；缩写（全大写短词/abbr 前缀）、专名
  （首字母大写）、领域标记条目一律**不产出义项**（宁可留空让人补，也不写「滤水器」）。
- 二次「包含匹配」兜底加两道闸：被匹配词 ≤ id 长度的 2 倍、只认词首/词尾包含、
  结果命中 `JUNK_GLOSS_RE`（医/俚/人名/姓氏/的复数…）就丢弃。
- 新增**出厂自检**：义项可疑的刷屏警告；兜不出来的写进
  `.work/derived/morphemes-needing-gloss.json`（152 条待补，见第五节）。
- 已实际重跑过：自动义项 1954 条、可疑从 57 → 21（剩余全是已进覆盖表的）。

### 2. 覆盖表 `scripts/lib/morpheme-fallback.mjs`（本轮核心，408 条）
- `OVERRIDE_MEANINGS` 358 → **408 条**，分五组，每组判据写死在注释里：
  - 第一轮 96 条（保留）
  - A 领域标记泄漏（165 条严判名单）
  - B 缩写/专名（`so`=自旋轨道分裂、`mary`=玛丽女子名、`coun`=顾问…）
  - C 包含匹配（`wandn`、`eond`、`ular`…）
  - D 义项与家族词对不上（`car`=关心、`bull`=球、`fly`=飞、`relate`=关联、`line`=线、列…）
  - E 词性段拼接（`any`、`employ`、`murder`、`boy`、`daughter`… 44 条，判据是机器给的：
    同一条 ECDICT 记录用旧实现算一遍、新实现算一遍，产物==旧实现 就是那个 bug 的产物）
- 新增 `OVERRIDE_DISPLAY`（6 条）：`wf`→`wo`、`wer`→`wor`、`eond`→`yond`、`deh`→`de`、
  `pr`→`pre`、`eth`→`th` —— 卡片画的是 displayText，`wf＝女人` 这种牌不能再出现。
- 新增 `INJECT_MORPHEMES`（7 条）：`missan`(错过)、`wander`(漫步)、`whether`(是否)、
  `carrot`(胡萝卜)、`isol`(岛)、`corr`(共同、加强)、`delivery`(递送)，每条写明来历。
- `DICT_ARTIFACT_RE` 去掉行首锚点；新增 `looksLikeDuplicatedGloss()`（重复片段判定，
  **先剥 `……` 和标点**，否则 ic/ant/ist/an 这些正确后缀会被误报）。

### 3. 收敛点 `scripts/tools/build-stage3-config.mjs`
- 新增 `SPLIT_REPLACE`（5 词，整条切分替换；WORD_PART_FIX 只能改 id，救不了切错的分区）：
  `wander / whether / carrot / delivery` → 单词根；`isolate` → `isol + ate`。
  库里本就有 15 个单部件词（porter/audio/grade/judge/script…），整词当词根是既定形态。
- `WORD_PART_FIX` 扩 3 条：`correct/correlate` 的 `cor → corr`（cor- 是 com- 的同化形式，
  不是「心」）；`missing` 的 `miss → missan`（古英语 missan，不是 mittere）。
  ⚠️ 这里的 key 必须写**切分表里的原始 id**——`missing` 写成 `mit` 会静默失效。
- 新增注入逻辑（INJECT_MORPHEMES）+ `OVERRIDE_DISPLAY` 应用。
- 日志：整条切分替换 5 处、注入词素 7 个、义项覆盖 387 处、显示名修正 6 处。

### 4. cigen 对照 `scripts/lib/id-canon.mjs`
- `['cor','courage']` → `['cor','courage','corr']`（不认 corr 会把 correct/correlate 丢词）。
- `['mit','miss','mitt']` → `+ 'missan'`：**missan 与 mit 共用表面 miss**，21 号的
  「表面→id」Map 是后者胜出，cigen 的 `miss` 会被解析成 missan，dismiss 因此被判冲突丢词
  （实测丢词 1 个）。放进同一组后命中任一即算对上。

### 5. 闸门 `scripts/validate-content.mjs`
- **A20b**：`DICT_ARTIFACT_RE` + `looksLikeDuplicatedGloss` 双判据，去锚点（修掉盲区 1）。
- **A20c（新）**：干扰项池体检 —— 每个被抽进 `distractors` 的词素，义项命中判据就按词素
  聚合报警，指出它会出现在几个词的拼词盘上（修掉盲区 2）。当前 **0 条**。

### 6. 新增自检 `scripts/tools/check-morpheme-overrides.mjs`
覆盖表是「错值唯一修正点」，它自己出错就全盘皆错。查五件事：
① 重复 key（JS 对象字面量会**静默后者胜出**，必须从源码文本数，不能数对象）；
② 上游不存在的 id（覆盖静默失效）—— 上游从 stage1 + 各批文件取，**不能读
stage3-content.json**（那是产物，会让 INJECT 每次重跑都报重复）；
③ 值过 A20 且不是垃圾；④ 显示名撞名（豁免 MORPHEME_MERGE 会删掉的源 id，
从 build-stage3-config.mjs 源码里提取名单，避免两处手写漂移）；⑤ 注入记录字段齐全。
当前 **通过**（408 覆盖 / 6 显示名 / 7 注入）。

### 7. 产物已重跑
`npm run content:all` 已跑通：**2698 词 / 2146 词素 / 42 世界，切分通过 2698（丢 0 个）**，
词素 +7（注入）−5（whet/wandn/rn/very/iso）= 2146。抽查确认落位：
`boy=男孩`、`any=任何`、`employ=雇用`、`wf=女人(wo)`、`wer=人(wor)`、`grav=重`、`bute=给`、
`statistic=统计`、`physic=自然、身体`、`hydro=水`、`bull=球`、`car=关心`；
`correct=corr+rect`、`correlate=corr+relate`、`missing=missan+ing`、`wander/whether/carrot/delivery`
单词根、`isolate=isol+ate`、`dismiss=dis+mit`（丢词已救回）。

### 8. `content:all` 首次端到端跑通（60/70 号不再是摆设）
之前 `content:all` 一直死在 60 号：复核 handoff（`words-review.json`）只写了 Stage 1 那 300 词，
Stage 3 铺到 2698 词后整链在这一步硬失败，于是 **70 号报告停在 09-13 的「词条 300」、
复核报告 3.7 说的「2400/2698 缺复核」就是这么来的**。修法（`60-llm-review-content.mjs`）：

- **字段坏**（有记录但 check 缺项 / severity 非法 / 词表外的词）→ 仍然硬失败（数据被污染）；
- **整条缺记录** → 响亮地报告覆盖缺口（`2400/2698 个词没有复核记录`），按「未复核」放行
  并写进产物（`severity: 'unreviewed'`、`covered: false`），让 70 号如实报告覆盖率。
  理由与 21 号「丢词从硬失败改成报告后继续」同源：闸门常红比没有闸门更糟。

`70-report.mjs` 相应改为：标题从「Stage 1 产出报告」改成「内容产出报告」；复核行写
`覆盖 298/2698（未复核 2400）…隔离率 0.0%（按已复核词计）` —— 原先的「隔离率 0.0%」
读起来像全库复核过且没问题，其实只查了 300 词。报告当前实际数据：词 2698 / 词根 1845 /
gzip 767.6 kB / 漂移 0.0%。

### 9. 测试盲区补上（复核报告 3.6）
A20b / A20c / A24b 写在 `validate-content.mjs` 里，`npm test` 原本**完全跑不到** ——
本轮 A20b 漏了 10 条垃圾义项还报 0，靠的只有手动跑脚本。新增 `tests/gates.test.ts`：
用 `spawnSync` 把 `node scripts/validate-content.mjs` 当一条测试跑，断言退出码 0。
现在 **54/54**（原 53 + 1）。

### 10. A22 死变体修剪（88 → 2）
`legacy 12 个词素 + 各批各留各的变体表` 累计出 86 个「没有任何词用到」的死变体。
在 **40 号**（legacy 在这里才与 extraMorphemes 合并，只有这里能一次管全）加了统一修剪：
只保留「实际被词用到的表面」与「canary 干扰项用到的表面」两类。
两个关键取舍写死在注释里：
- **id / displayText 不保留**：A6 允许 displayText 作为表面、A15 按 id 查 morphemeById，
  都不依赖 allomorphs；而「id 本身是死变体」恰是铺库后最常见的形态
  （rotation 用 rotat、consumption 用 consump，id `rot`/`consume` 反而没人用）。
- `spec:spic` 这 2 条警告是 canary 锚点要用的变体，**必须保留**，属已知误报。

### 11. 811 条缺中文例句全部补齐（A1 811 → 0）
这 811 条全部是 `tatoeba-no-cmn`：英文句来自 Tatoeba 语料，但语料里**没有**中文对照句
（实测把 22 号的候选池上限从 3000 提到 15000，8 分钟一条都救不回来，已回退）。
修法：新增 handoff 第 4 层 `scripts/lib/handoff/words-examples-stage3b.json`（22 号的
`handoffPaths` 加了一层，同名词后者胜出），由复核 AI 逐条把现有英文例句翻译成中文 ——
**exampleEn 原样保留自语料，只补 exampleCn**。合并脚本带三道契约自检：
exampleEn 与语料逐字一致（防抄错）、exampleCn 无拉丁字母、exampleCn 有汉字。
（`_v_ex_part1~6.json` 是分批写的工作文件，合并后可删。）

## 四、验证结果（全部通过）

```bash
node scripts/tools/fix-distractors.mjs     # 义项池 1603 个，已补 1118 词 / 跳过 0
node scripts/30-llm-prose.mjs              # 释义通过 2682 个非 canary 词（契约全绿）
node scripts/40-assemble.mjs scripts/lib/stage3-content.json   # 2698 词 / 2146 词素 / 42 世界
node scripts/validate-content.mjs          # ✅ 内容校验通过，0 错误 / 138 警告
npm test                                   # ✅ 54/54 通过（5 个文件，含新增的脚本层闸门测试）
node scripts/tools/check-morpheme-overrides.mjs                # ✅ 覆盖表自检通过
npm run content:all                        # ✅ 端到端全绿（含 60/70 号，见第三节第 8 条）
```

警告构成：A23 难度梯度 127、A8 例句夹专名 9、A22 canary 锚点变体 2 —— 全部是已知的合理保留。
**A20b / A20c / A1 / A22 死变体均 0 命中**。

终验抽查（对照复核报告 2.1~2.6 点名的每一处）：

```
woman     = wf[wo]=女人 + mann[man]=人                  （曾是 wf=滤水器）
correct   = corr[cor]=共同、加强 + rect[rect]=直、正      （曾是 cor=心）
correlate = corr[cor]=共同、加强 + relate[relate]=关联    （曾是 cor=心 + relate=讲）
missing   = missan[miss]=错过 + ing[ing]=正在             （曾是 mit=送、放）
wander    = wander=漫步        （曾是 wandn=化暗杆内螺纹 + rn=走）
whether   = whether=是否       （曾是 whet=磨 + her=她）
carrot    = carrot=胡萝卜      （曾是 car=汽车 + rot=轮转）
isolate   = isol=岛 + ate=使…  （曾是 iso=相等 + late=携带）
delivery  = delivery=递送      （曾是 deli=熟食店 + very=真正的）
bulletin  = bullet=告示 + in   （曾是 bullet=子弹医弹）
trivial   = tri=三 + vial=路   （曾是 vial=小瓶装入小瓶）
audience 干扰项 = fresh / regis(统治) / lay / report           （曾是 whet/wf/wer）
干扰项牌面垃圾义项 = 0（修复前 101 条 / 99 词 / 20 词素）
```

本轮排查用的临时脚本（`_v_*.mjs`、`_v_audit.txt`、旧 draft/quality 备份）已清理 ——
其中的判据都已永久落进 `validate-content.mjs`（A20b/A20c）与
`check-morpheme-overrides.mjs`，不需要再靠临时脚本复查。

## 五、剩余待办

1. ~~文档修正~~ **已完成**：
   - `docs/Stage3-进度交接.md`：状态表三处数字改对（2146 词素 / A1 811 才是警告大头 /
     例句覆盖 1887/2698），并新增「二点五、词素义项全表复核」一节；
   - `docs/复核报告-3adf204至8e721f7.md`：文末追加「四、复核处置记录」，
     更正 2.1 的方法描述、3.4 的算式（89−13=76≠74）、3.7 的表述（70/60 号是 Stage 1 遗留），
     并补记四类新发现与修复结果。
2. **复核覆盖缺口（60 号）**：2400 个词没有复核记录（handoff 只写了 Stage 1 的 300 词）。
   管线现在会响亮报告并放行，但**这些词没经过 13 项对抗式复核** —— 补齐
   `scripts/lib/handoff/words-review.json` 是内容质量上真正欠的债（每词 13 个布尔检查，
   适合交给 LLM 批量跑一遍再人工抽查）。
3. ~~152 条待补义项~~ **已完成**：`build-morpheme-table.mjs` 里内嵌的那份 `FALLBACK_MEANINGS`
   **漏了六级批新增**（kin/let/safe/acquisite/vergere…），是「FALLBACK 有两份」这个老问题
   真正咬人的时刻 —— 152 个词素在草稿里义项为空。已删掉内嵌份、统一 import 共用表，
   并把 152 条补进共用表（126 条抄现库产物值，26 条不在现库的按词源给定），另补
   `just`/`metre`/`vs`/`ards` 四条防回退。现在草稿义项为空 **0** 条，
   且草稿里 20 条可疑义项全部被 `OVERRIDE_MEANINGS` 接管 ——
   将来重建批次不会再死在 A20，也不会再把垃圾义项带进产物。
   （`fusc`/`vinci` 两个草稿专属 id 不进覆盖表：从未进任何批次，写了也轮不到，
   由生成器的出厂警告盯着。）
4. **`morpheme-quality.json` 已过期**：根因修复后重跑过 build-morpheme-table，
   `scripts/.work/derived/morpheme-quality.json`（分级 A1–A4）是旧版，别再当判据用。
5. ~~1035 警告~~ **已收敛到 138 条，且全是合理保留**：A23 难度梯度 127（选题，要补 d1/d5
   家族词）、A8 例句夹专名 9（Tatoeba 语料如此，句首专名被豁免）、A22 canary 锚点变体 2
   （为保住 16 个回归锚点必须保留）。
6. **残留的语义错挂（已记录、未处理）**：
   - `adjust = ad + jud`：词源是 ad+juxta(靠近)，但改成「jud=判断、法律」教学上尚可接受；
   - `blunder = blend + er`、`fragrant = fra + grant`、`steward = ste + ward`、
     `grammar/nightmare` 里的 `-mar`、`carbon` 里的 `-bon`：同形异源，**不是义项能修的**，
     要动切分才会波及别的词，本轮明确不动（已在覆盖表注释里标注）；
   - `sid=坐`（consider）、`der=剩下`（remainder）：复核报告 3.2/3.3 判「证据弱但不算错」，保留。

## 六、改动文件清单（git 可见）

```
M scripts/lib/id-canon.mjs                 # cigen 别组：+corr、+missan
M scripts/lib/morpheme-fallback.mjs        # 核心：408 条覆盖 + 6 显示名 + 7 注入 + 双判据
M scripts/tools/build-morpheme-table.mjs   # 根因：shortMeaning/deriveMeaning + 出厂自检
M scripts/tools/build-stage3-config.mjs    # 收敛点：SPLIT_REPLACE + WORD_PART_FIX + 注入/显示名
M scripts/validate-content.mjs             # A20b 重写 + A20c 新增
M scripts/lib/stage3-content.json          # 产物（收敛点重写）
M src/domain/content/*.json + data.ts      # 产物（40 号重写，2698/2146）
?? scripts/tools/check-morpheme-overrides.mjs  # 新增自检
?? docs/复核报告-3adf204至8e721f7.md           # 另一个 AI 的复核报告（未提交）
```
