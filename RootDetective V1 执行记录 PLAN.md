# 词根侦探 RootDetective V1 执行记录 PLAN

> 依据：`词根侦探 RootDetective V1 设计方案.md` V1.1
> 建立日期：2026-09-10
> 最后复核：2026-09-18（见第 13 节的当日记录）
> 当前仓库状态：核心闭环已可玩并稳定运行，词库 3192 词 / 2284 词素 / 42 世界（远超原定 129 词素 / 1000 词）；
> 已补齐 PWA 离线壳与发音资源管线；尚未接入 Supabase 账号同步，尚无统计页/成就页/设置页。
> 目标：先做出可玩的“拆解 → 三段推义 → 验证 → 奖励 → 复习”闭环，再扩展到 129 个主词素、1000+ 单词、PWA、同步和音频。
>
> **读法提醒**：第 4 节的 M0–M7 勾选清单是**当初的计划条目**（多数没有回填），
> 真实进度一律以 **2.5 节「里程碑实际进度」** 为准，别对着勾选框判断现状。

---

## 0. 执行结论

### 产品判断

这不是普通单词卡，而是“以词根为记忆主体、以单词为迁移测试载体”的推理型学习工具。V1 最重要的不是页面数量，而是证明下面这件事成立：

```text
用户学会一个词根
→ 能拆解熟悉词
→ 能把词根迁移到同族新词
→ 猜错后能回到词根解释
→ 复习系统优先考新词而不是重复答案
```

### 执行策略

1. 先做一条端到端垂直切片，不先铺满 129 个词素。
2. 所有玩法状态先用本地数据跑通，再接 Supabase 同步。
3. 内容以 JSON/CSV 为单一来源，数据库只是发布产物。
4. 词源解释与助记解释分开存储，禁止用“看起来能拆”替代真实词源审核。
5. 每个阶段都必须有可运行产物和验收证据，不以“代码写完”作为完成标准。

### V1 必须完成

- 词素变体归并模型。
- 15 个后缀模型。
- 拆解题：正确词素 + 形似/义似/随机干扰项。
- 三段推义：字面义、语义方向、现代释义揭晓。
- 验证页：词源、助记、音标、例句、同族词。
- 词根级复习排程与分层题池。
- 难度权重修正后的稳定度更新。
- 首页、侦探页、复习页、图鉴页、统计页的最小可用版本。
- PWA 离线壳、音频播放与缓存。
- 基础账号同步、每日任务、成就。
- 至少 12 个词素、60 个词的垂直切片；最终扩展到 129 个主词素、1000+ 单词。

### V1 暂不阻塞上线

- 订阅支付和完整商业化流程。
- Capacitor 原生上架。
- 真人录音。
- 自定义词库的复杂编辑器。
- 大规模社交、排行榜、多人玩法。

---

## 1. 当前状态与基线

> 下表是 2026-09-18 的复核结果，不再是立项时的快照。

| 项目 | 状态 | 记录 |
|---|---|---|
| 设计方案 | 已完成 | V1.1，包含玩法、数据、API、页面、技术栈、里程碑 |
| 前端工程 | 已完成 | Vite 8 + React 19 + TS 7；`npm run dev/build/test` 三条命令均通过 |
| 后端/数据库 | 未开始 | 没有 Supabase 项目与迁移；`src/data/repositories.ts` 已抽出仓库层作为前置，页面不直接碰存储 |
| 词库内容 | 已超额完成 | 结构化管线（10→40 号脚本）产出 3192 词 / 2284 词素 / 42 世界，`validate:content` 零警告 |
| 音频 | 部分完成 | `npm run audio:build` 可批量生成发音并压成 mp3；目前只录了样批（3 词），全量尚未生成 |
| 自动化测试 | 进行中 | 71 个用例覆盖领域逻辑、持久化、内容规则、管线、PWA、仓库层；**尚无端到端测试与浏览器回归** |
| 部署 | 未开始 | Vercel/Netlify 与 Supabase 待定；PWA 已具备离线能力，可先静态托管 |
| 离线/PWA | 已完成 | `public/manifest.webmanifest` + `public/sw.js`，生产构建自动注册，断网可继续答题 |

### 第一个可交付目标

在本地浏览器中完成一次完整流程：

```text
选择词根 → 拆解单词 → 完成三段推义 → 查看验证 → 获得奖励 → 进入复习 → 用同词根新词测试
```

建议垂直切片内容：

- 前缀：`pre-`、`re-`、`circum-`、`in-/im-`。
- 词根：`spec`、`dict`、`port`、`vid`、`mit`、`duc`、`voc`、`grad`。
- 后缀：`-ion`、`-ive`、`-able`、`-ity`。
- 词数：教学词 24 个、迁移测试词 24 个、进阶词 12 个。

这组内容能覆盖变体、同化、后缀、语义梯子、干扰项和复习降级，不追求一开始覆盖全部词表。

---

## 2. 建议技术基线

### 工程

- 前端：React + TypeScript + Vite。
- 样式：Tailwind CSS，先建立颜色、间距、排版和响应式断点的基础 token。
- 状态：Zustand；侦探流程用显式状态机或 reducer，避免页面组件各自维护流程状态。
- 交互：移动端点选，桌面端拖拽；`dnd-kit` 只用于桌面增强。
- 动画：Framer Motion，仅用于步骤切换、反馈和奖励，不影响答题可用性。
- 音频：Howler；音频资源缺失时必须有静默降级。
- 本地存储：IndexedDB，建议封装为 repository，不让页面直接操作数据库。

### 后端

- Supabase Auth + Postgres + Storage + Edge Functions。
- RLS：用户进度、复习日志、成就只允许本人读写；公开词库只读。
- 服务器 API 与本地 repository 使用同一领域类型，避免前后端重复定义。

### 代码边界

```text
src/domain/       词素、单词、题型、评分、排程等纯业务逻辑
src/data/         本地 JSON、IndexedDB、Supabase repository
src/features/     首页、侦探、复习、图鉴、统计、成就
src/components/   可复用 UI
src/audio/        播放、预加载、缓存
src/lib/          配置、日志、埋点、工具函数
supabase/         migrations、seed、functions
scripts/          内容校验、音频生成、导入导出
tests/            domain、repository、e2e
```

### 关键决策记录

| 决策 | 选择 | 原因 |
|---|---|---|
| 内容真源 | `data/*.json` + CSV 导入 | 可审核、可 diff、可重建数据库 |
| 干扰项 | 单独关系表为真源，接口返回组合结果 | 支持类型、难度、去重和人工覆盖 |
| 复习算法 | `Scheduler` 接口 + FSRS 适配器 | 便于先测业务权重，再替换实现 |
| 离线优先 | 本地读写，联网后同步 | 核心玩法不依赖网络 |
| 词根进度 | 以 `morpheme_id` 为唯一排程对象 | 符合“复习词根，不复习单词” |
| 付费 | V1 先保留能力开关，不阻塞核心玩法 | 避免支付系统拖慢学习闭环验证 |

---

## 2.5 里程碑实际进度（2026-09-18 复核）

这张表取代第 4 节那些没回填的勾选框：状态、证据、缺口都写在这里。

| 里程碑 | 状态 | 已落的产物 | 还没做 |
|---|---|---|---|
| M0 工程基线与领域模型 | 已完成 | Vite/React/TS、`src/domain/types.ts` 领域类型、`PuzzleStage` 状态机、5 个纯函数测试起桩 | Tailwind / Zustand（决策：维持朴素 CSS，见下方决策记录）；独立 ADR 文档 |
| M1 核心侦探垂直切片 | 已完成 | 取证→字面义→现代义→结案四步闭环、词素卡点选、同化变体提示、针对性错因诊断、调试/复用/复习三种模式 | 桌面端拖拽（现为点选为主）、刷新后恢复最后一题 |
| M2 内容管线与词库扩展 | 已超额完成 | 10→40 号脚本管线产出 3192 词 / 2284 词素 / 42 世界，`validate:content` 零警告 | 词源真伪的人工复核——目前只有 `npm run audit:etymology` 的机器预筛（474 条线索待核） |
| M3 词根复习与权重 | 已完成 | 以词根（而非单词）排程、到期队列、难度修正的熟练度、迁移率统计、配对复习、结构性缺门词根降级排队 | 备选题型降级链（逆向造词 / 完形 / 听写）、跨时区与并发同步的测试 |
| M4 页面与导航闭环 | 部分完成 | 四页可用视图：今天 / 拼单词 / 复习 / 词根地图；连胜、洞察点、等级、世界解锁 | **统计页、成就页、设置页尚未实现**；无键盘操作与对比度的系统扫描 |
| M5 PWA、离线、音频 | 部分完成 | manifest + Service Worker + 生产环境自动注册；发音生成脚本（SAPI 离线合成 → ffmpeg 压 mp3）、索引优先/浏览器合成兜底的播放降级 | 发音只录了样批（3 词），全量 3192 词未生成；进度仍在 localStorage，未迁到 IndexedDB |
| M6 Supabase 同步与账号 | 未开始 | **已抽出 `src/domain/repository.ts` 抽象层作为前置**（三个接口语义同步、落地好了本地适配器与 6 个契约测试） | 建库迁移、RLS、登录、匿名进度合并、幂等键——均待 Supabase 项目就位 |
| M7 内容扩容与发布验收 | 部分完成 | 词库规模远超 RC 门禁（129 词素 / 1000+ 词）；`npm run content:all` 可重建；`docs/词源审核报告.md` 已产出 | 桌面 Chrome / 移动 Safari 的真机回归、部署与环境变量、小规模试用 |

## 3. 里程碑总表

预计总周期：3-4 周；以下按连续工作日估算，内容审核可与开发并行。

| 编号 | 阶段 | 预计 | 主要产物 | 完成标志 |
|---|---|---:|---|---|
| M0 | 工程基线与领域模型 | 1 天 | 可运行项目、类型、测试框架、ADR | `npm run dev` 和基础测试通过 |
| M1 | 核心侦探垂直切片 | 3-4 天 | 拆解、三段推义、验证、奖励 | 能完成一局并刷新结果 |
| M2 | 内容管线与词库扩展 | 4-6 天 | 12 词素/60 词种子、校验器、导入脚本 | 数据校验零阻断错误 |
| M3 | 词根复习与 FSRS 权重 | 3-4 天 | 分层题池、降级、迁移率、排程 | 猜对/猜错影响符合规格 |
| M4 | 页面与导航闭环 | 4-5 天 | 首页、侦探、复习、图鉴、统计、成就 | 移动/桌面主要流程可用 |
| M5 | PWA、离线、音频 | 3-4 天 | 安装、离线词库、Edge-TTS、缓存 | 断网仍可完成已下载任务 |
| M6 | Supabase 同步与账号 | 2-3 天 | 登录、同步、RLS、冲突策略 | 两端登录后进度一致 |
| M7 | 内容扩容与发布验收 | 4-6 天 | 129 词素、1000+ 词、部署、回归 | 22 节验收清单完成 |

---

## 4. 逐阶段执行清单

> 下面是**立项时列的条目**，绝大多数没有回填勾选框，不要用它判断现状。
> 每个里程碑的真实状态、产物和缺口见 **2.5 节**。

### M0：工程基线与领域模型（第 1 天）

- [x] 初始化 Vite + React + TypeScript。（vite 8 + react 19 + ts，`npm install` 通过）
- [~] 加入 Tailwind、Zustand、测试框架、基础 lint/format。（vitest 已接入；Tailwind/Zustand 用普通 CSS 占位，后续按需引入）
- [x] 建立目录边界和环境变量模板。（`src/domain/`、`tests/`、`scripts/` 已建立；环境变量模板后置到接 Supabase 时）
- [x] 定义领域类型：`Morpheme`、`Word`、`WordPart`、`ReviewProgress`、`SessionStats`。（`src/domain/types.ts`）
- [x] 定义侦探状态机：`split → literal → metaphor → verify → reward`。（`PuzzleStage` 于 `src/domain/types.ts`，流程在 `src/App.tsx`）
- [ ] 定义 repository 接口：`ContentRepository`、`ProgressRepository`、`AudioRepository`。（当前用 `src/domain/data.ts` 内存数据直出，接 IndexedDB/Supabase 时抽象）
- [x] 写第一批纯函数测试：词素拼接、顺序校验、同化提示、答题结果映射。（`tests/logic.test.ts` 3 个用例通过）
- [ ] 记录 ADR：离线优先、词根排程、干扰项关系表、FSRS 适配器。（决策已写在 PLAN 第 2 节，独立 ADR 文档后置）

**验收证据**

- 本地启动页能显示“RootDetective”。
- 至少 5 个领域测试通过。
- 没有页面直接依赖 `localStorage` 或数据库细节。

### M1：核心侦探垂直切片（第 2-5 天）

- [ ] 实现线索区：目标词、音标、词性、难度。
- [ ] 实现词素卡区和槽位区。
- [ ] 实现正确词素、形似干扰、义似干扰、随机干扰项。
- [ ] 实现移动端点选和桌面端拖拽两种输入。
- [ ] 实现同化标签，例如 `im-` 是 `in-` 的变体。
- [ ] 实现第 1 阶：词素拼接，必须精确匹配。
- [ ] 实现第 2 阶：语义方向三选一，随机化选项顺序。
- [ ] 实现第 3 阶：现代释义揭晓，不计入推理得分。
- [ ] 实现验证面板：词源解释、助记解释、三层语义、例句、同族词、音频按钮。
- [ ] 实现奖励反馈：洞察点、词根经验、连续答对计数。
- [ ] 处理错误、重试、跳过、返回上一步和刷新页面恢复。

**验收证据**

- `circumspect` 或等价测试词能走完 7 个状态。
- 答错第 1/2 阶时能给针对性反馈，不能直接跳过关键步骤。
- 刷新页面后不会把已经完成的题错误地标记为完成或丢失。

### M2：内容管线与词库扩展（第 4-10 天，并行）

- [ ] 建立 `morphemes.json`、`words.json`、`word_morphemes.csv`、`distractors.json`。
- [ ] 将主词素、变体、颜色、词源、等级结构化。
- [ ] 将 15 个后缀全部建模，并标明词性/状态作用。
- [ ] 为每个词填写：拆分、实际表面形式、词源解释、助记解释、literal、metaphor、modern、词性、音标、例句、难度。
- [ ] 生成词素家族池：`teaching`、`first_test`、`advanced`。
- [ ] 编写内容校验器：引用存在、词素顺序连续、主词素唯一、变体归属、字段完整、干扰项不含正确答案。
- [ ] 编写重复词、重复释义、重复音频文件名和孤立词素检查。
- [ ] 先完成垂直切片 12 词素/60 词，再扩展到完整 129/1000+。
- [ ] 对高风险词做人工审核：硬拆、同源误判、助记冒充词源、现代义偏移。

**验收证据**

- `npm run validate:content` 在切片数据上通过。
- 每个切片词素至少有 3 个迁移词；最终每个核心词根至少 8 个家族词。
- 输出一份内容审核报告，记录阻断项与已接受的助记解释。

### M3：词根复习与 FSRS 权重（第 8-13 天）

- [ ] 以 `user_id + morpheme_id` 建立唯一进度记录。
- [ ] 实现状态：`new → learning → reviewing → mastered`。
- [ ] 实现按词根取队列，不按单词重复排程。
- [ ] 实现题池优先级：教学词 → 迁移测试词 → 进阶挑战词。
- [ ] 记录 `tested_word_ids`、迁移正确数、尝试数、响应时长。
- [ ] 实现弹药耗尽降级：逆向造词 → 情境完形 → 词素辨析 → 听写/速认。
- [ ] 定义 `Scheduler` 接口并接入 FSRS；在结果层应用单词难度权重。
- [ ] 实现难度映射：基础 K=1、中等 K=3、高难 K=5。
- [ ] 实现示例规则：基础词猜错重罚，高难词猜错轻罚；具体系数写入配置并可调。
- [ ] 计算北极星指标：同词根新词首次猜对次数 / 总尝试次数。
- [ ] 为跨天、重复提交、并发同步和时区写测试。

**验收证据**

- 同一词根复习时优先出现未测试的新词。
- 迁移词耗尽后能自动进入降级题型，不出现空队列死路。
- 基础词和高难词的猜错结果对稳定度影响不同，且有自动化断言。

### M4：页面与导航闭环（第 11-17 天）

- [ ] 首页：今日破解、继续学习、复习队列、世界入口、连续天数、洞察点。
- [ ] 侦探页：将 M1 的核心交互整理为响应式工作区。
- [ ] 复习页：到期词根、题型层级、错题回炉、复习统计。
- [ ] 图鉴页：词根树、家族词、已破解词、未解锁世界。
- [ ] 统计页：掌握词根、陌生词猜对率、迁移率、7 日留存所需事件数据。
- [ ] 成就页：首次破解、连续 7 天、100 词、30 词根、迁移大师、世界解锁。
- [ ] 设置页：音频、主题、离线下载、账号入口；订阅先展示能力开关。
- [ ] 处理空状态、加载状态、错误状态、无网络状态和首次使用引导。
- [ ] 做移动端触控尺寸、键盘操作、颜色对比度和读屏标签检查。

**验收证据**

- 320px 宽和桌面宽度下，核心控件不重叠、不溢出。
- 从首页开始可在 3 次点击内进入今日破解。
- 侦探流程中不需要依赖浏览器后退才能恢复。

### M5：PWA、离线、音频（第 15-19 天）

- [ ] 添加 manifest、service worker、安装图标和版本号。
- [ ] 缓存应用壳、切片词库、最近题目和必要字体/图标。
- [ ] IndexedDB 保存进度、复习日志、音频 Blob 和待同步事件。
- [ ] 编写 Edge-TTS 批量生成脚本，支持并发限制、重试、跳过已存在文件。
- [ ] 将音频上传到 Storage/CDN，并为词库生成 `audio_url`。
- [ ] Howler 播放层支持预加载、缓存、加载失败降级和手动重播。
- [ ] 离线时允许完成已下载题目；联网后上传事件并去重。
- [ ] 增加缓存版本升级和旧数据迁移策略。

**验收证据**

- 首次在线打开后断网，仍能完成一个已下载复习会话。
- 音频加载失败不阻塞答题。
- 清除缓存、刷新、重新联网后，进度不会重复记账。

### M6：Supabase 同步与账号（第 18-21 天）

- [ ] 创建迁移：词素、单词、词素关联、干扰项、家族池、进度、复习日志、成就。
- [ ] 添加唯一键、外键、索引和 `updated_at`。
- [ ] 开启 RLS：公开词库只读；用户表按 `auth.uid()` 隔离。
- [ ] 实现邮箱/魔法链接或现有可用登录方式。
- [ ] 实现本地匿名进度与登录账号合并规则：按事件时间和幂等键合并，不直接覆盖较新记录。
- [ ] 实现 `get_daily_session`、`get_review_queue`、`submit_split`、`submit_inference`、`sync_progress` 等最小 API。
- [ ] 给提交接口加请求幂等键，避免重复点击造成重复奖励。
- [ ] 记录关键事件：开始题目、完成拆解、推义结果、复习结果、音频播放、会话完成。

**验收证据**

- 两个浏览器登录同一账号，刷新后能看到一致进度。
- 普通用户无法读取或写入其他用户的进度和日志。
- 离线事件重放后奖励与复习次数只计算一次。

### M7：内容扩容与发布验收（第 22-28 天）

- [ ] 完成 40 个前缀、74 个词根、15 个后缀的主词素数据。
- [ ] 完成 1000+ 单词；为每个词配置完整字段和难度。
- [ ] 生成全部干扰项、家族池和音频清单。
- [ ] 运行内容校验、数据库导入、音频完整性和孤立引用检查。
- [ ] 完成桌面 Chrome、移动 Safari/Chrome 的核心流程回归。
- [ ] 完成 PWA 安装、离线、恢复、同步、登出/重新登录测试。
- [ ] 部署前端和 Supabase，配置生产环境变量和错误监控。
- [ ] 组织小规模试用，重点观察“先拆还是先查”和迁移率。
- [ ] 根据试用结果只做阻断性修复，不在上线前继续扩展功能面。

**验收证据**

- 第 5 节验收清单全部打勾。
- 有一份发布候选版本记录：版本号、数据库迁移号、词库版本、音频版本。
- 可从全新设备完成注册、学习、复习、断网、重新联网和同步。

---

## 5. 数据与接口的最小实现顺序

### 先建数据，再建页面

1. `morphemes`：主词素、类型、含义、变体、词源、颜色、等级。
2. `words`：词、音标、词性、三层释义、例句、难度、音频。
3. `word_morphemes`：实际表面形式、位置、同化标记。
4. `word_distractors`：干扰项类型和人工覆盖。
5. `morpheme_family_pool`：题池层级和使用次数。
6. `user_morpheme_progress`：词根级状态、稳定度、迁移统计。
7. `review_logs`：每次结果和响应时间。
8. `achievements` / `user_achievements`：成就定义与解锁记录。

### API 分批落地

**第一批：本地直接实现同契约**

- `get_daily_session`
- `get_review_queue`
- `submit_split`
- `submit_inference`
- `get_word_family`
- `get_distractors`

**第二批：接入账号与同步**

- `sync_progress`
- `unlock_world`
- `claim_daily_reward`
- `get_audio_url`

接口响应必须携带 `content_version` 和 `server_time`，便于缓存、迁移和排程调试。

---

## 6. 测试计划

### 领域单元测试

- 词素顺序和完整性。
- 变体映射与同化提示。
- 干扰项不泄漏答案。
- 三段推义判分。
- 难度权重对稳定度的影响。
- 题池耗尽和降级顺序。
- 迁移率、连续答对、成就条件。
- 重复提交幂等。

### 集成测试

- 本地 repository 与 IndexedDB 的读写。
- Supabase migration、RLS、同步合并。
- 音频 URL、缓存、失败降级。
- service worker 更新与旧缓存迁移。

### 端到端测试

- 新用户完成首局。
- 答错第 1 阶、第 2 阶后重试。
- 复习拿到同词根新词。
- 题池耗尽后进入降级题型。
- 断网完成已缓存会话，恢复网络后同步。
- 登录后跨设备看到同样的进度。

### 手工体验检查

- 触控目标不小于可用尺寸。
- 不依赖颜色单独表达正确/错误。
- 键盘可完成桌面端核心流程。
- 语义选项不因长度造成布局跳动。
- 加载、错误和空状态可理解且不阻断任务。

---

## 7. 每日执行记录格式

每次实际开发后，在本文件底部追加一条记录，不只写“完成”，要留下可复核证据。

```md
### YYYY-MM-DD / Mx

- 今日目标：
- 已完成：
- 产物/文件：
- 验证命令：
- 验证结果：
- 未完成：
- 阻塞与决定：
- 下一步：
```

### 状态约定

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成且有验证证据
- `[!]` 被阻塞，需要明确决定或外部资源

### 完成定义（Definition of Done）

一个任务只有同时满足以下条件才标记 `[x]`：

1. 代码/内容/配置已落盘。
2. 至少有一条自动化测试或可复现手工步骤。
3. 关键边界条件已验证。
4. 相关文档或数据版本已更新。
5. 不引入已知阻断性错误。

---

## 8. 首轮执行顺序（现在就做）

### 第 1 个工作日

- [ ] 初始化前端工程和测试框架。
- [ ] 建立领域类型与状态机。
- [ ] 放入 1 个完整示例词和 3 个干扰项。
- [ ] 先让“拆解 → 推义 → 验证”在本地跑通。

### 第 2 个工作日

- [ ] 完成侦探页可用布局。
- [ ] 完成点选交互和桌面拖拽。
- [ ] 完成第 1/2/3 阶判分、反馈和重试。
- [ ] 加入领域测试和首个端到端测试。

### 第 3 个工作日

- [ ] 接入 12 个词素/60 个词切片数据。
- [ ] 加入家族词与奖励。
- [ ] 加入本地复习队列和最小排程。
- [ ] 形成第一个可给人试玩的版本。

### 第 4-5 个工作日

- [ ] 加入复习分层、降级和迁移率统计。
- [ ] 做移动/桌面体验修正。
- [ ] 冻结垂直切片交互，再开始批量扩容内容。

---

## 9. 风险看板

| 风险 | 触发信号 | 处理动作 | 负责人/状态 |
|---|---|---|---|
| 词源硬拆 | 审核者无法确认构词关系 | 标记为助记或剔除，不进入核心题 | 待定 |
| 内容扩张拖慢开发 | 129/1000+ 迟迟未能试玩 | 冻结扩容，先交付 12/60 垂直切片 | 待定 |
| 复习算法不稳定 | 难词错误导致词根被过度降级 | 先走配置化权重，增加回归样例 | 待定 |
| 离线数据冲突 | 同一事件重复奖励 | 事件幂等键 + 时间戳合并 | 待定 |
| 拖拽在移动端难用 | 触控误操作或无法完成 | 移动端保持点选为主 | 待定 |
| 题目被答案记忆污染 | 新词迁移率低、旧词正确率高 | 扩充家族池，优先新词，不重复旧词 | 待定 |
| 音频服务失败 | 批量生成缺文件或播放失败 | 生成清单、重试、播放降级为无音频 | 待定 |
| 订阅阻塞上线 | 支付接入超出排期 | 保留能力开关，后置支付 | 待定 |

---

## 10. 首次评审问题

在 M1 垂直切片完成后，只回答以下问题，不提前扩展功能：

1. 用户是否理解“先拆再猜”而不是直接猜答案？
2. 第 1 阶拆解是否比普通翻卡更有参与感？
3. 第 2 阶语义方向是否真的帮助推理，而不是纯靠排除？
4. 验证页是否清楚区分词源解释和助记解释？
5. 用户在看到同词根新词时，是否能迁移至少一部分含义？
6. 错题回到词根卡时，是否能解释错因？
7. 移动端点选和桌面拖拽是否都能在不看说明的情况下完成？

评审结果必须写进每日记录，并决定：继续扩容、调整玩法，或缩减范围。

---

## 11. 版本门禁

### Alpha 门禁

- 1 个词根完整闭环可玩。
- 领域单元测试通过。
- 本地刷新和错误重试不丢状态。

### Beta 门禁

- 12 个词素、60 个词通过内容校验。
- 复习、降级、迁移率可观测。
- 移动/桌面核心流程通过。
- PWA 离线会话可用。

### Release Candidate 门禁

- 129 个主词素、1000+ 单词导入成功。
- 音频清单无关键缺失。
- Supabase RLS、同步、幂等和恢复测试通过。
- 第 22 节验收标准全部完成或明确标记为后置项。

---

## 12. 当前下一步

> 本节写于立项日，当时的「下一步」早已做完了。2026-09-18 复核后的状态见 **2.5 节**，
> 当日证据见第 13 节的最后一条记录。以下保留原始文字作为决策沿革。

**下一步不是继续改方案，而是执行 M0：初始化工程、建立领域模型、让一个词跑通完整闭环。**

第一条实际记录应在完成 M0 后追加，并至少包含：

- 项目启动命令。
- 目录结构。
- 首个示例词及其拆解数据。
- 通过的测试命令。
- 仍未决定的技术问题。

---

## 13. 每日执行记录

### 2026-09-10 / M4（案件远征玩法升级收尾）

- 今日目标：把垂直切片收敛为可理解、可持续的案件循环，补齐真实成长、复习排程、本地档案、世界解锁和无障碍关键路径。
- 已完成：
  - `PlayerProfile` 统一保存 XP、洞察点、词根进度、已结案件、学习日和首局引导状态；档案通过版本化 JSON 安全加载、规范化并写回 localStorage。
  - 案件室保留 `split → literal → metaphor → verify → reward` 内部状态机，用户界面显示“取证 → 拼出字面义 → 推理现代词义 → 结案验证”四步；证据槽未填满时原生禁用提交，词素卡/语义选项提供 `aria-pressed` 和可见选择状态。
  - 错误不扣 XP，降低词根稳定度并提前进入复习；完成案件按普通/复习模式结算 XP、洞察点、迁移和连续学习日，并用事件 ID 防止当前会话重复提交。
  - 首页增加“开始第一宗案件”和首局说明；首案完成后折叠教学卡。复习页、图鉴页使用真实派生数据，传讯港按等级、案件数和观察词根稳定度动态解锁。
  - 复习队列明确为：`learning` 词根直接显示；`reviewing` 词根仅在 `dueAt <= now` 时显示；`new`/`mastered` 不入队列。
  - 持久化层拒绝无效 ISO 时间戳和不存在的自然日，保留合法当前数据集 ID 并去重。
  - 补充等级、稳定度边界、到期排程、连续自然日、奖励、错误/完成结算、世界解锁和日期规范化测试；提升关键辅助文本对比度，恢复 `.page-section` 宽度约束。
- 产物/文件：`src/App.tsx`、`src/domain/logic.ts`、`src/domain/persistence.ts`、`src/styles.css`、`tests/logic.test.ts`、`tests/persistence.test.ts`、`RootDetective V1 执行记录 PLAN.md`。
- 验证命令：
  - `npm test`
  - `npm run validate:content`
  - `npm run build`
- 验证结果：
  - `npm test`：3 个测试文件、31 个用例全部通过。
  - `npm run validate:content`：16 个种子单词、12 个词素、4 条词根初始进度通过。
  - `npm run build`：`tsc -b && vite build` 成功，JS gzip 约 82.65 KB；npm 的 `python`/`sass-binary-site` 配置警告不阻断验证。
- 未完成：浏览器手工流程、axe/键盘/读屏扫描、320px/375px 实际溢出检查；桌面拖拽、StatsView、音频播放、PWA、账号同步和 60 词扩容仍为后置项。
- 阻塞与决定：无阻塞。复习页采用到期队列语义，避免把尚未到期的 reviewing 词根误显示为待复习；`learning` 项即使缺少有效 `dueAt` 也保留在队列，确保错误后线索不丢失。
- 下一步：启动开发服务器做浏览器验收；若核心交互无阻断，再按优先级补键盘/axe 检查和后置功能。



### 2026-09-10 / M0（基线可复现验证）

- 今日目标：验证 M0 基线是否可复现，修复工程化缺口，使测试/校验/构建三条命令全部绿灯。
- 已完成：
  - 依赖安装：`npm install`（43 包），补装 `@types/react`、`@types/react-dom`（缺失导致 `tsc -b` 报 538 个 TS7016/TS7026/TS7006 错误）。
  - 修复 `src/main.tsx` 中 `import './styles.css'` 的 TS2882：在 `tsconfig.app.json` 增加 `"types": ["vite/client"]`。
  - 三条验证命令全部通过（见下方验证结果）。
- 产物/文件：`package.json`（新增 devDependencies：@types/react、@types/react-dom）、`tsconfig.app.json`（types: ["vite/client"]）、`dist/` 构建产物。
- 验证命令：
  - `npm test`
  - `npm run validate:content`
  - `npm run build`
- 验证结果：
  - `npm test`：1 个测试文件、3 个用例全部通过（词素顺序、难度权重、迁移率）。
  - `npm run validate:content`：`Content validation passed: 6 seed words, 5 field groups.`
  - `npm run build`：`tsc -b && vite build` 成功，产物 `dist/index.html` + `dist/assets/*`（gzip 后 JS 约 77KB）。
- 未完成：
  - repository 接口抽象（`ContentRepository` / `ProgressRepository` / `AudioRepository`）。
  - 独立 ADR 文档。
  - Tailwind / Zustand / lint / format 接入。
- 阻塞与决定：无阻塞。决定维持 KISS：领域逻辑先行，UI 库与状态库在 M1 闭环稳定后再引入。
- 下一步：M1 核心侦探垂直切片（拆解 → 三段推义 → 验证 → 奖励完整走通 `circumspect`），补 repository 接口与 ADR。

### 2026-09-10 / M1（垂直切片内容与玩法完善）

- 今日目标：扩充切片内容到可复习规模，补齐第 2 阶选项随机化、同化提示、题池挑选与幂等提交等 M1 清单项。
- 已完成：
  - 词库扩充：12 词素（4 前缀 + 4 词根 + 4 后缀）/ 16 单词，4 个词根家族（spec / dict / port / vid）各 4 词，全部词素可诚实拆解（遵守“不硬拆”规则，`porter` 只考 port 词根）。
  - 领域逻辑新增：`shuffledMetaphorOptions`（第 2 阶选项随机化）、`isMetaphorCorrect`、`assimilationHint`（同化提示）、`pickNextWord`（优先未测试新词）、`isDuplicateSubmit`（幂等提交）、`shuffle`（Fisher-Yates）。
  - 领域测试扩至 16 个：拆解判分、三选一、同化提示、难度权重（对/错双向）、streak、题池挑选、幂等、洗牌、以及内容完整性（干扰项不泄漏答案、家族引用存在、词素引用与位置连续）。
  - App 交互修复：主词根改按 type=root 选取；metaphor 阶段渲染洗牌后的选项；答错重试保持同一套选项；下一题经 `pickNextWord` 优先未测试新词。
  - 校验器扩展：检查 16 词、12 词素、7 组字段。
  - 修正两处数据建模错误：`porter` 的 -er 不再硬挂到 `ity`；`ity` 家族由 `visibility` 正常承载。
- 产物/文件：`src/domain/data.ts`、`src/domain/logic.ts`、`tests/logic.test.ts`、`scripts/validate-content.mjs`、`src/App.tsx`。
- 验证命令与结果：
  - `npm test`：16/16 通过。
  - `npm run validate:content`：`Content validation passed: 16 seed words, 12 morphemes, 7 field groups.`
  - `npm run build`：`tsc -b && vite build` 成功（JS gzip 约 80KB）。
- 未完成：桌面端拖拽（当前点选为主）、刷新页面状态恢复、repository 接口、独立 ADR。
- 阻塞与决定：无阻塞。`-er` 等未建模后缀先不出现在 parts 中，避免“看起来能拆”的硬拆；后续建模 -er/-or 后缀族再补。
- 下一步：M2 内容管线（JSON 单一来源 + 完整校验器）或先做 M1 剩余（拖拽、状态恢复）；建议先让真实用户走一遍 `circumspect` 闭环再扩。

### 2026-09-18 / M2+M5+M6 前置（内容收口、PWA、发音与仓库层）

- 今日目标：把上一轮 A23 结构性缺口的吵闹清干净，并把方案里一直挂着没动的三块基础能力（离线、发音、同步前置）落地。
- 已完成：
  - **A23 收尾**：`d1 缺入门词`的结构性缺口白名单机制，与既有 d5 机制完全对称（`options.rootD1StructuralGap` + `build-a23-d5-gap.mjs` 同口径产出 `scripts/gates/a23-d1-structural-gap.json`）。能补的词根一律不进表、继续报警；现在 `validate:content` **0 个警告**。
  - **词库扩容**：批次 08 落地，words 3063→3192、morphemes 2236→2284。
  - **A8 回归**：`22-choose-examples` 与 `morpheme-fallback` 修补例句选取与词素回退，例句错配清零。
  - **PWA 离线**：新增 `public/manifest.webmanifest` 与 `public/sw.js`（缓存壳 + 详情分片按需缓存 + 按版本号清旧缓存），生产构建才注册以免影响 HMR；`tests/pwa.test.ts` 静态检查这四件事没被漏改。
  - **repository 抽象层**：新增 `src/domain/repository.ts`（Content/Progress/Audio 三个接口）与 `src/data/repositories.ts` 本地适配器，App 改为整包解构接入（调用点零改动）。接口刻意做成同步——这是「离线优先」决策的落地形态，见文件内注释。
  - **发音管线**：新增 `scripts/tools/80-build-audio.mjs`（`npm run audio:build`），用 Windows SAPI 离线合成后经 ffmpeg 压成 mp3（85KB→10KB/词），产出 `audio-index.json`；播放端「音频文件优先、浏览器合成兜底」。**注：方案里写的 Edge-TTS 暂未实现**——它要走外网 WebSocket，而本机外网依赖代理、Node 原生 WebSocket 不支持代理，脚本里留了 `--engine=edge` 的位置。
  - **词源预筛**：新增 `scripts/tools/audit-etymology-risk.mjs`（`npm run audit:etymology`），扫出 474 条线索并按可信度分层，产出 `docs/词源审核报告.md`。**机器只做预筛，词源真伪仍需人工翻词典。**
  - **卫生清理**：根目录 116 个调试残留归入 `_scratch/`（可随时删）并补进 `.gitignore`，根目录从 116 个文件降到 18 个。
  - **结构性缺门词根降级**：`scripts/tools/build-non-teaching-roots.mjs` 从 d1 缺口表算出 15 个降级词根（家族连一个入门词都没有），复习队列把它们排到最后而非跳过。
- 产物/文件：`src/domain/contentRules.ts`、`scripts/validate-content.mjs`、`scripts/tools/build-a23-d5-gap.mjs`、`scripts/gates/a23-d1-structural-gap.json`、`public/manifest.webmanifest`、`public/sw.js`、`src/main.tsx`、`index.html`、`src/domain/repository.ts`、`src/data/repositories.ts`、`src/App.tsx`、`src/domain/logic.ts`、`scripts/tools/80-build-audio.mjs`、`scripts/tools/build-non-teaching-roots.mjs`、`scripts/tools/audit-etymology-risk.mjs`、`.gitignore`、`package.json`。
- 验证命令与结果：
  - `npx tsc -b`：通过（0 错误）。
  - `npx vitest run`：**8 个文件 / 71 个用例全部通过**（新增 PWA 6 + repository 6 + 降级 3）。
  - `npm run validate:content`：**3192 词 / 2284 词素 / 42 世界，0 个警告**。
  - `npm run build`：通过，`dist` 含 manifest 与 sw.js。
  - `node scripts/tools/80-build-audio.mjs --limit=3`：实测写出 3 个 mp3（约 10KB/词）与索引条目。
  - `node scripts/tools/audit-etymology-risk.mjs`：扫描 3192 词，标记 474 条线索。
- 未完成：
  - 统计页、成就页、设置页三个 M4 视图。
  - 发音全量生成（3192 词约 32MB，尚未决定要不要全部入盘）。
  - Supabase 迁移、RLS、登录与跨端同步（仓库层前置已就绪）。
  - Edge-TTS 引擎（受限于本机外网需要代理）。
  - 274 条机器线索里的人工复核。
  - 浏览器端到端回归、axe/对比度扫描、桌面拖拽。
- 阻塞与决定：
  - 决定发音默认用 SAPI 而非 Edge-TTS：前者离线可复现，后者在当前网络条件下跑不通。
  - 决定 repository 接口保持同步语义：改成 Promise 会让 App 初始化多出「档案未就绪」中间态，收益不抵复杂度，且与「离线优先」决策一致。
  - 决定词审计只做预筛并公开交代误报率：`词源口径不一致` 一项 355 条多为误报（真实词源常引更深处源头，如 `critical` 引希腊语 krinein），报告里已标注可信度分层。
- 下一步：先补 M4 缺失的三个页面（统计/成就/设置），或先录全量发音；两者互不依赖。
  若要接 Supabase，下一步是给 `src/data/` 加一份远端适配器，并按 `src/domain/repository.ts` 的语义做同步与冲突合并。

