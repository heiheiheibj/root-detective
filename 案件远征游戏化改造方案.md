# RootDetective「案件远征」游戏化改造方案

## Context

当前 `http://localhost:8890/` 是 RootDetective「词根侦探」背单词游戏。现有页面已有暗色案件室视觉、词根线索板和核心流程，但首次进入时用户不知道第一步，阶段按钮与反馈不够明确；侧栏的等级、复习数量、连续天数等部分数据仍是硬编码或未形成真实成长闭环；颜色层级偏弱，自动化无障碍扫描发现小字号低对比度问题。

项目已有两份 Markdown 设计资料，明确产品不是普通单词卡，而是“以词根为记忆主体、以单词为迁移测试载体”的推理游戏。本方案在其基础上收敛为一条轻 RPG 路线，先解决首局可理解性、交互反馈、视觉层级和真实成长，再扩展内容。

参考资料：
- [词根侦探 V1 设计方案](词根侦探%20RootDetective%20V1%20设计方案.md)
- [RootDetective V1 执行记录](RootDetective%20V1%20执行记录%20PLAN.md)

## 推荐方向：轻 RPG「案件远征」

不做射击游戏，采用“案件调查”主题：用户是侦探；每个词根是一块语义区域；每个单词是一宗案件；前缀、词根、后缀是证据；拆解、推义、验证是调查步骤；XP 和词根熟练度构成成长；完成词根家族后解锁新的语义世界。

核心循环：

```text
接案 → 查看线索 → 拼证据 → 形成字面画面 → 选择语义方向
→ 查看结案报告 → 获得 XP/洞察点 → 挑战同族新词 → 复习或解锁新世界
```

保留现有领域状态机 `split → literal → metaphor → verify → reward`，但 UI 对用户呈现为四步：

```text
1 取证 → 2 拼出字面义 → 3 推理现代词义 → 4 结案验证
```

`literal` 作为解释内容，不让用户完成无意义的二次提交。答错不扣生命、不显示失败页；错误只影响熟练度或复习安排，并给出可行动提示。

## 首次用户 60 秒体验

1. **0—5 秒：明确入口**
   - 首页首屏只突出一个主按钮：“开始第一宗案件”。
   - 按钮下方写清：“点词素卡排成单词，先拆，再猜，最后验证。”
   - 默认继续使用 `circumspect` 作为教学案件。
2. **5—10 秒：显示三步说明**
   - `① 拼证据　② 推语义　③ 看结案`。
   - 当前步骤高亮，完成步骤显示勾选；首局完成后可折叠。
3. **10—25 秒：完成拆解**
   - 明确显示“点选构成这个词的证据，按从左到右放入槽位”。
   - 空槽位显示“第 1 块”“第 2 块”，未填满时按钮禁用并说明还需几张。
   - 选中卡片使用 `aria-pressed` 和文字状态，不只变色。
4. **25—35 秒：理解字面义**
   - 直接展示 `circum（周围） + spect（看） = 从四周观察`。
   - 主按钮为“继续推理”，说明字面画面如何走向现代词义。
5. **35—48 秒：选择语义方向**
   - 三个选项统一高度和文案结构，主按钮为“锁定推理”。
   - 错误提示说明哪个方向不完整、缺少哪条语义桥，允许立即重试。
6. **48—57 秒：结案验证**
   - 直接显示现代释义、词源解释、助记解释、例句和词根熟练度变化。
   - 明确区分“词源解释”和“助记解释”，不把助记冒充真实词源。
7. **57—60 秒：奖励和连续行动**
   - 显示 `+XP`、`+洞察点`、熟练度前后值和下一条同族词。
   - 主按钮为“挑战同族新词”，次按钮为“稍后复习”，不把用户送回空白首页。

## 页面信息架构

主导航控制为四项：

1. **今日案件**：今日案件、继续学习、到期复习、当前等级、XP、连续学习和世界入口。首屏只有一个主要 CTA。
2. **案件室**：目标词、步骤条、词素卡、语义选项、结案报告、奖励和证据板。桌面双栏，移动端证据板折叠。
3. **复习队列**：仅显示 `dueAt <= now` 或学习中的词根；每行显示熟练度、下一条未测试家族词和预计时长。
4. **词根世界**：世界章节、词根节点、家族词、进行中/已掌握/锁定状态和明确解锁条件。

“侦探档案”作为进度统计页内分区，收纳等级、迁移率、成就、已破解单词和连续学习。设置放入头像/更多菜单，不占用首版主导航。

## 成长和奖励模型

只保留两种跨页面可见的成长值，避免多货币造成理解负担。

### 侦探 XP

XP 跨词根累计，等级由纯函数推导，禁止在 UI 硬编码“等级 04”。

| 等级 | 累计 XP | 称号 |
|---:|---:|---|
| 1 | 0 | 新手侦探 |
| 2 | 100 | 见习侦探 |
| 3 | 250 | 线索分析员 |
| 4 | 450 | 语义调查员 |
| 5 | 700 | 词根侦探 |
| 6 | 1000 | 家族追踪者 |
| 7 | 1400 | 迁移专家 |

一宗完整案件最多 80 XP：完成拆解 15、完成推理 15、完成验证 10、首次尝试完成 20、成功迁移 20。答错不扣 XP；重复尝试只获得基础完成奖励，避免鼓励刷分。

### 洞察点

用于轻量奖励和未来提示能力预留，不在首版加入复杂商城：完成案件 +24，完成复习 +12，每日三宗 +60，连续 3/7 天 +20/+50。首版提示免费一次，等验证提示价值后再决定是否消费洞察点。

### 词根熟练度

继续使用现有 `stability: 0—100`，不新增重复的词根等级：0—24 为“正在建立线索”，25—49 为“正在复习”，50—79 为“已能迁移”，80—100 为“已掌握”。`new` 表示未尝试，不显示成“待解锁”。

### 世界解锁

先用现有 16 个单词做两个真实世界：

- **观察所**：`spec`、`vid`，默认开放，负责教学。
- **传讯港**：`dict`、`port`；等级 3、完成 3 宗案件、且任一观察词根熟练度达到 40% 后开放。

锁定卡必须写出条件，例如：“再完成 2 宗案件，并将一个观察词根提升到 40%，即可解锁传讯港。”复习已见词根不受世界锁定影响。

首版做 6 个成就：初次结案、连续推理、首次迁移、观察入门、七日侦探、世界开门。每个成就显示当前进度、下一步行动和奖励。
