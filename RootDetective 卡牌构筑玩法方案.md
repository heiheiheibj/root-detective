# RootDetective「卡牌构筑 + 配对复习」玩法方案

> 状态：方案待评审（未改代码）
> 前置资料：[词根侦探 V1 设计方案](词根侦探%20RootDetective%20V1%20设计方案.md)、[案件远征游戏化改造方案](案件远征游戏化改造方案.md)
> 本文覆盖并取代《案件远征》中的玩法与交互部分，内容模型、词素表、复习排程公式继续沿用旧文档。

---

## 1. 为什么要改

### 1.1 内容模型没问题

现有数据是对的，不用重做：

- 12 个词素（4 前缀 / 4 词根 / 4 后缀），16 个单词，全部遵守"不硬拆"规则（[data.ts](src/domain/data.ts)）。
- `Word.parts` 本身就是"组合"模型：每个词 = 有序的模块列表（[types.ts:25-30](src/domain/types.ts#L25-L30)）。
- 判分按词素 ID 顺序精确匹配（[logic.ts:94](src/domain/logic.ts#L94)）。
- 熟练度、排程、奖励、幂等提交都是可用的纯函数。

**问题在呈现层，不在领域层。**

### 1.2 真正的毛病

**毛病一：两套比喻打架。**

| 层 | 用的话术 |
|---|---|
| 侧栏导航 | 今日构建 / 编译器 / 调试器 / 回归测试 |
| 案件室内 | 取证 / 结案 / 线索板 / 证据 |

用户每屏都要解码"这个词在这里指什么"。对背单词的人，"编译器""回归测试"是纯噪音，不产生任何语义锚点。

**毛病二：一屏之外还有一屏。**

一个词走 5 个状态（`split → literal → metaphor → verify → reward`，[types.ts:3](src/domain/types.ts#L3)）。其中：

- `literal` 是零决策的纯阅读页，还要点一次"继续推理"（[App.tsx:194](src/App.tsx#L194)）。
- `verify` 也是零决策的纯阅读页，点"完成结案"（[App.tsx:196](src/App.tsx#L196)）。

4 步里只有 2 步有决策。其余是"翻页"。按 60 秒/词算，16 个词约 16 分钟见底，中间大量在点按钮。

**毛病三：答错跳页。**

```ts
// App.tsx:95
setActiveView('debugger')
```

拆解答错、推义答错都直接把用户踢到"调试器"独立页面。这像被打回工具台，不像游戏里的反馈。而且跳页会丢掉当前上下文——用户看不到自己刚才错在哪一格。

**毛病四：解锁条件写成规格说明。**

"任一观察词根熟练度达到 40%"（[data.ts:169](src/domain/data.ts#L169)）——这是给开发看的，不是给玩家看的。

---

## 2. 选定方向

| 模式 | 玩法 | 定位 |
|---|---|---|
| **主线** | 卡牌构筑（出牌拼词） | 学新词。慢，有决策，有推理 |
| **复习** | 词素 / 词族配对（连连看） | 刷熟练度。快，高频，低认知成本 |

不做射击、限时、Roguelike 进主线。理由：这些玩法的时间压力与"看见词根、推理词义"的认知过程冲突——玩家会退化成靠排除法抢答，学不到结构。

**主题建议：保留现有暗色案件室视觉，只换词汇。** 不做整套美术翻新。侦探是轻风味，卡牌是交互骨架。

---

## 3. 主线：卡牌构筑

### 3.1 一句话

**词素就是卡。手牌 = 候选词素，拼装槽 = 词形，开炉 = 判分。**

一个动作贯穿全局：**出牌**。用户不需要理解任何抽象阶段名。

### 3.2 单关布局（一案一屏，全程不跳页）

```text
┌──────────────────────────────────────────────────┐
│  circumspect          ●●○ 进阶      熟练度 ▓▓▓░░ │  ← 顶栏
├──────────────────────────────────────────────────┤
│                                                  │
│        拼装槽                                    │
│      ┌─────────┐ ┌─────────┐                     │
│      │ circum  │ │  spect  │                     │  ← 出牌区
│      └─────────┘ └─────────┘                     │
│           周围   +     看                        │  ← 实时字面义
│                                                  │
│      合成预览：从四周观察                         │  ← 原 literal 阶段
│                                                  │
├──────────────────────────────────────────────────┤
│  手牌                                            │
│  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐        │  ← 手牌区
│  │ pre- ││circum││ port ││ spec ││ -ion │        │
│  │ 前   ││ 周围 ││ 携带 ││ 看   ││ 名词 │        │
│  └──────┘└──────┘└──────┘└──────┘└──────┘        │
└──────────────────────────────────────────────────┘
```

现有 [App.tsx:193](src/App.tsx#L193) 的 `morpheme-grid` + `assembly-slots` 已经是这个结构，**改造主要是手感与合并区块，不是重建**。

### 3.3 核心循环

```text
摸牌 → 出牌 → 开炉 → 淬火 → 结算
 ↓      ↓      ↓      ↓      ↓
发卡   点卡入槽  判分   三选一  发奖励
```

**① 摸牌**
系统发 `n` 张正确卡 + `2~3` 张干扰卡。`n = word.parts.length`。
复用 `getAvailableCards(word)`（[App.tsx:173-177](src/App.tsx#L173-L177)），数据现成。

**② 出牌**
点卡即入槽，按从左到右排。可点已入槽的卡撤回。
出满 `n` 张才能点"开炉"。未满时按钮禁用并显示"还差 N 张"（现有逻辑已实现，[App.tsx:193](src/App.tsx#L193)）。

**③ 开炉（原 `split` 判分）**
复用 `isSplitCorrect`。

- **对** → 卡片合并动画，槽位下方**立刻显示字面义**（`circum 周围 + spect 看 = 从四周观察`）。这一步吃掉了原 `literal` 整屏。
- **错** → **不跳页**。错误卡片震动回手，槽位清空对应格，顶部提示条给出可行动指引：
  > 第 2 块不对。spect 是"看"，但它不是这个位置的形式。

  同时记 `applyIncorrectAttempt`（[logic.ts:241](src/domain/logic.ts#L241)），熟练度下降。
  连续错 2 次 → 高亮一张正确卡作为援助。

**④ 淬火（原 `metaphor` 三选一）**
字面义下方展开 3 张语义卡。复用 `shuffledMetaphorOptions` / `isMetaphorCorrect`。

- **对** → 卡牌翻面，显示现代词义 + 词源解释 + 助记解释 + 例句。这一步吃掉了原 `verify` 整屏。
- **错** → 原地换色 + 一句话诊断，**可立刻重选**，不跳页（现有 `createMetaphorDiagnosis` 复用，[logic.ts:78](src/domain/logic.ts#L78)）。

**⑤ 结算（原 `reward`）**
**整段保留**。熟练度前后对比、XP 拆解、洞察点、"挑战同族新词"按钮——这块设计是好的（[App.tsx:197](src/App.tsx#L197)）。

### 3.4 关键改动汇总

| 改动 | 从 | 到 |
|---|---|---|
| 流程长度 | 4 步 + 2 个纯阅读页 | 2 次决策 + 1 次结算 |
| 答错 | 跳转到"调试器"独立页 | 原地反馈，不跳页 |
| `literal` | 独立屏 + "继续推理"按钮 | 合成成功时同屏显示 |
| `verify` | 独立屏 + "完成结案"按钮 | 淬火成功的翻面内容 |
| 调试器页 | 顶级导航项 | **删除**，诊断并入关卡提示条 |

### 3.5 状态机

建议收敛为三态（[types.ts:3](src/domain/types.ts#L3)）：

```ts
export type PuzzleStage = 'build' | 'forge' | 'reward'
```

映射关系：

| 旧 | 新 | 说明 |
|---|---|---|
| `split` + `literal` | `build` | 出牌 + 合成 + 显示字面义 |
| `metaphor` + `verify` | `forge` | 三选一 + 揭示现代义 |
| `reward` | `reward` | 不变 |

`CaseMode`（`compiler` / `debugger` / `regression`）**不再暴露给用户**。内部保留，用于奖励计算和提示显隐：[logic.ts:225-239](src/domain/logic.ts#L225-L239) 的奖励公式不动。

---

## 4. 复习：配对（连连看）

### 4.1 为什么不做第二遍卡牌

复习目标是刷 [logic.ts:189](src/domain/logic.ts#L189) 的 `stability`，不是学新词。卡牌流程约 60 秒/词，太重，用户会跳过复习。配对约 15 秒/轮，能高频刷。

### 4.2 棋盘

```text
┌────────── 复习：spec（看）──────────────┐
│  连击 x3        进度 4/8        ⏱ 42s   │
│                                          │
│  ┌────────┐    ┌────────┐               │
│  │  spec  │    │   说   │               │
│  └────────┘    └────────┘               │
│  ┌────────┐    ┌────────┐               │
│  │   看   │    │  dict  │               │
│  └────────┘    └────────┘               │
│                                          │
│        点两个卡配对，全对清场            │
└──────────────────────────────────────────┘
```

左列与右列独立打乱，玩家点两个卡。匹配成功 → 两卡淡出 + 连击 +1；不匹配 → 抖动复位，连击归零。

### 4.3 两档难度

| 档位 | 左列 | 右列 | 计入 `migrationCorrect` |
|---|---|---|---|
| **热身** | 词素字面形式（`spec`） | 含义（`看`） | 否 |
| **迁移** | 单词（`circumspect`） | 现代义（`谨慎的`） | **是** |

迁移档才是真正的复习测试——它检验的是"看见词根能不能推出新词义"，与主线目标一致。

数据现成：热身档用 `Morpheme.displayText ↔ Morpheme.meaningCn`；迁移档用 `Word.word ↔ Word.modernMeaningCn`，词族索引走 `getFamilyWords`（[data.ts:193](src/domain/data.ts#L193)）。

### 4.4 判分接入

一轮全部配对完成 → 对每个涉及的词根调用一次 `updateProgress(progress, word, correct)`（[logic.ts:189](src/domain/logic.ts#L189)）。

- 热身档：不计入 `migrationAttempts`，只刷展示，避免污染迁移正确率指标。
- 迁移档：正常计入。
- 错配不额外惩罚（抖动复位已经是反馈），但**连击归零**作为软惩罚。

复习入口沿用现有"到期队列"逻辑（`getReviewQueue`，[logic.ts:207](src/domain/logic.ts#L207)），只换呈现。

---

## 5. 页面信息架构

导航从 5 项减到 4 项：

| 现在 | 改成 | 内容 |
|---|---|---|
| 今日构建 | **今日** | 主 CTA「开始一关」+ 到期复习入口 + 等级 / XP / 连击 |
| 编译器 | **工坊** | 卡牌关卡，一屏到底 |
| 调试器 | — | **删除** |
| 回归测试 | **配对** | 连连看复习 |
| 词根图谱 | **图谱** | 词根世界与解锁，保留 |

**"调试器"整页删除。** 答错原地反馈后，独立诊断页没有存在意义。原 `DebugDiagnosis` 面板（[App.tsx:192](src/App.tsx#L192)）降级为关卡顶部的提示条。

### 解锁条件改写为玩家语言

| 现在（规格说明） | 改成（玩家语言） |
|---|---|
| `达到等级 3（当前等级 2）` | 再升 1 级 |
| `再完成 2 宗不同案件` | 再通关 2 个词 |
| `将一个观察词根提升至 40%（当前最高 22%）` | 把"看"练到 40%（当前 22%） |

词根用 `meaningCn`（"看"）而不是 `displayText`（"spec / spect"）指代——玩家先记住的是含义。

---

## 6. 数据改动清单

**好消息：主线和复习都几乎不需要改数据。**

### 复用（不动）

- `Word.parts` / `distractors` / `metaphorOptions` / `literalMeaningCn` / `modernMeaningCn` / `exampleEn` / `exampleCn`
- `Morpheme.displayText` / `meaningCn` / `color`
- `isSplitCorrect` / `shuffledMetaphorOptions` / `createSplitDiagnosis` / `createMetaphorDiagnosis`
- `updateProgress` / `calculateCaseReward` / `applyCompletedCase` / `getReviewQueue` / `getLevelInfo`
- 幂等提交 `isDuplicateSubmit`

### 新增

| 项 | 位置 | 用途 |
|---|---|---|
| `PuzzleStage` 改三态 | [types.ts:3](src/domain/types.ts#L3) | `build \| forge \| reward` |
| `MatchMode` 类型 | [types.ts](src/domain/types.ts) | `'warmup' \| 'transfer'` |
| 配对题生成函数 | [logic.ts](src/domain/logic.ts) | `buildMatchBoard({ morphemes \| words, count })` |
| 连击 / 计数状态 | App 层 | 复习模式局内状态，不入档案 |

### 删除 / 降级

| 项 | 处理 |
|---|---|
| `MODE_STRATEGIES.debugger` | 逻辑保留（奖励计算要用），UI 分支删除（[logic.ts:16-25](src/domain/logic.ts#L16-L25)） |
| `MODE_STRATEGIES.regression` | 同上，改为配对模式的内部标记（[logic.ts:26-35](src/domain/logic.ts#L26-L35)） |
| `DebugDiagnosis.expected` / `.selected` | "目标结构 / 你的输入"是开发者味字段，改写为玩家语言或删除（[types.ts:102-109](src/domain/types.ts#L102-L109)） |
| `navItems` 中的 `debugger` | 删除（[App.tsx:7-12](src/App.tsx#L7-L12)） |

---

## 7. 分阶段实施

| 阶段 | 内容 | 验收 |
|---|---|---|
| **1. 垂直切片** | spec 家族 4 词跑通卡牌流程（`build → forge → reward`），改状态机、改判分后的 UI 呈现、答错原地反馈 | 4 个词能完整通关，无跳页，无"调试器" |
| **2. 复习 + 导航** | 配对模式两档难度 + 导航改 4 项 + 删调试器页 + 解锁文案改写 | 复习一轮 ≤ 30 秒，迁移档计入 `migrationCorrect` |
| **3. 打磨** | 出牌/合并/翻面动画、答错震动、连击反馈、无障碍复查 | `npm run test` 全绿；无动画降级路径可用 |

---

## 8. 风险

| 风险 | 影响 | 应对 |
|---|---|---|
| **改 `PuzzleStage` 会碰到测试** | [tests/logic.test.ts:45,50](tests/logic.test.ts#L45) 断言 `split.stage === 'split'`、`metaphor.stage === 'metaphor'` | 阶段 1 先改测试再改类型；`DebugDiagnosis.stage` 的取值同步更新 |
| **卡牌动画在移动端掉帧** | 低端机出牌卡顿 | 动画是增强不是必需；保留现有 `aria-pressed` + 文字状态（"已选中/未选中"）作为降级路径 |
| **配对模式刷分** | 熟练度虚高 | 热身档不计 `migrationAttempts`；迁移档才计入 |
| **16 词见底快** | 阶段 1 就会遇到 | 本方案只验证手感。扩内容走旧文档的词素总表（[设计方案 §6](词根侦探%20RootDetective%20V1%20设计方案.md)）|
| **主题摇摆** | 一会儿侦探一会儿卡牌 | 定死：**卡牌是交互骨架，侦探是轻风味**。UI 词汇全部用卡牌语（出牌 / 手牌 / 开炉 / 淬火 / 合成），不再出现编译器、调试器、构建 |

---

## 9. 不做什么

- **不做自由文本输入**。理由同旧文档 §9.3：拼写错误会污染语义理解判断。
- **不做多货币**。只保留 XP + 洞察点，见旧文档成长模型。
- **不做射击 / 限时玩法进主线**。时间压力与词根推理的认知过程冲突。
- **不扩充词素数据**。先用现有 12 词素 / 16 词验证手感，手感对了再扩。
- **不做整套美术重做**。换词汇，不换视觉体系。
